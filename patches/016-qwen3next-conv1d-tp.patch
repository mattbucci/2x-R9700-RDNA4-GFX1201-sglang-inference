diff --git a/python/sglang/srt/configs/qwen3_5.py b/python/sglang/srt/configs/qwen3_5.py
index 98de12f7e..35252ef9b 100644
--- a/python/sglang/srt/configs/qwen3_5.py
+++ b/python/sglang/srt/configs/qwen3_5.py
@@ -1,5 +1,10 @@
 from transformers import PretrainedConfig
 
+from sglang.srt.configs.mamba_utils import (
+    Mamba2CacheParams,
+    Mamba2StateShape,
+    mamba2_state_dtype,
+)
 from sglang.srt.configs.qwen3_next import Qwen3NextConfig
 from sglang.srt.configs.qwen3_vl import Qwen3VLVisionConfig
 
@@ -33,6 +38,26 @@ class Qwen3_5TextConfig(Qwen3NextConfig):
         # Keep both names for compatibility with model code paths that read either.
         self.rope_parameters = rope_parameters or self.rope_scaling
 
+    @property
+    def mamba2_cache_params(self) -> Mamba2CacheParams:
+        # Qwen3.5 RDNA4 fix replicates DeltaNet projections (attn_tp_size=1 in
+        # models/qwen3_5.py) to avoid FP16 rounding drift through the recurrent
+        # state.  The SSM/conv cache must therefore match the full (unsplit)
+        # dimensions.  Qwen3-Next (Coder-Next 80B) uses the upstream sharded
+        # path — see Qwen3NextConfig.mamba2_cache_params.
+        shape = Mamba2StateShape.create(
+            tp_world_size=1,
+            intermediate_size=self.linear_value_head_dim * self.linear_num_value_heads,
+            n_groups=self.linear_num_key_heads,
+            num_heads=self.linear_num_value_heads,
+            head_dim=self.linear_value_head_dim,
+            state_size=self.linear_key_head_dim,
+            conv_kernel=self.linear_conv_kernel_dim,
+        )
+        return Mamba2CacheParams(
+            shape=shape, layers=self.linear_layer_ids, dtype=mamba2_state_dtype(self)
+        )
+
 
 class Qwen3_5Config(PretrainedConfig):
     r"""
diff --git a/python/sglang/srt/configs/qwen3_next.py b/python/sglang/srt/configs/qwen3_next.py
index 3bf153a22..ba8fb1912 100644
--- a/python/sglang/srt/configs/qwen3_next.py
+++ b/python/sglang/srt/configs/qwen3_next.py
@@ -287,6 +287,10 @@ class Qwen3NextConfig(PretrainedConfig):
             world_size = get_attention_tp_size()
             adjust_tp_num_heads_if_necessary(self, world_size, False)
 
+        # Qwen3-Next (Coder-Next 80B) keeps the upstream TP-sharded DeltaNet
+        # (ColumnParallelLinear at attn_tp_size).  Qwen3.5 subclasses override
+        # this property to force tp_world_size=1 because their DeltaNet is
+        # replicated by the RDNA4 FP16 precision fix.
         shape = Mamba2StateShape.create(
             tp_world_size=get_attention_tp_size(),
             intermediate_size=self.linear_value_head_dim * self.linear_num_value_heads,
