diff --git a/python/sglang/kernels/aot/setup_rocm.py b/python/sglang/kernels/aot/setup_rocm.py
index 471882f9b..600835884 100644
--- a/python/sglang/kernels/aot/setup_rocm.py
+++ b/python/sglang/kernels/aot/setup_rocm.py
@@ -75,12 +75,18 @@ if torch.cuda.is_available():
 else:
     print(f"Warning: torch.cuda not available. Using default target: {amdgpu_target}")
 
-if amdgpu_target not in ["gfx942", "gfx950", "gfx1250"]:
+_supported_targets = ["gfx942", "gfx950", "gfx1250"]
+# RDNA4 (gfx1200, gfx1201, etc.)
+_is_rdna4_target = amdgpu_target.startswith("gfx12") and amdgpu_target != "gfx1250"
+if _is_rdna4_target:
+    _supported_targets.append(amdgpu_target)
+if amdgpu_target not in _supported_targets:
     print(
-        f"Warning: Unsupported GPU architecture detected '{amdgpu_target}'. Expected 'gfx942', 'gfx950', or 'gfx1250'."
+        f"Warning: Unsupported GPU architecture detected '{amdgpu_target}'. Expected one of {_supported_targets}."
     )
     sys.exit(1)
 
+# gfx942 (MI300) uses FNUZ FP8; everything else (gfx950, gfx1250, gfx12xx) uses E4M3
 fp8_macro = (
     "-DHIP_FP8_TYPE_FNUZ" if amdgpu_target == "gfx942" else "-DHIP_FP8_TYPE_E4M3"
 )  # gfx950 and gfx1250 use E4M3
@@ -90,7 +96,13 @@ fp8_macro = (
 #   (leaves room for static shared allocations in the kernel).
 # - gfx95x (MI350) and gfx1250: LDS is larger. Large dynamic budget wastes LDS
 #   and pins occupancy to 1 block/CU. Keep it small (40KB) for better occupancy.
-topk_dynamic_smem_bytes = 48 * 1024 if amdgpu_target == "gfx942" else 40 * 1024
+# - gfx12xx (RDNA4): LDS is 128KB per WGP but 48KB is safe for compatibility.
+if amdgpu_target == "gfx942":
+    topk_dynamic_smem_bytes = 48 * 1024
+elif _is_rdna4_target:
+    topk_dynamic_smem_bytes = 48 * 1024
+else:
+    topk_dynamic_smem_bytes = 40 * 1024
 
 hipcc_flags = [
     "-DNDEBUG",
