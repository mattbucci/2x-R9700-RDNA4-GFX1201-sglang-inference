diff --git a/python/sglang/srt/configs/qwen3_5.py b/python/sglang/srt/configs/qwen3_5.py
index fddf783ab..66403ec7b 100644
--- a/python/sglang/srt/configs/qwen3_5.py
+++ b/python/sglang/srt/configs/qwen3_5.py
@@ -1,7 +1,16 @@
 from transformers import PretrainedConfig
 
+from sglang.srt.configs.mamba_utils import (
+    Mamba2CacheParams,
+    Mamba2StateShape,
+    mamba2_state_dtype,
+)
 from sglang.srt.configs.qwen3_next import Qwen3NextConfig
+from sglang.srt.configs.update_config import adjust_tp_num_heads_if_necessary
 from sglang.srt.configs.qwen3_vl import Qwen3VLVisionConfig
+from sglang.srt.utils import is_cpu
+
+_is_cpu = is_cpu()
 
 
 class Qwen3_5VisionConfig(Qwen3VLVisionConfig):
@@ -30,6 +39,33 @@ class Qwen3_5TextConfig(Qwen3NextConfig):
         # Keep both names for compatibility with model code paths that read either.
         self.rope_parameters = rope_parameters or self.rope_scaling
 
+    @property
+    def mamba2_cache_params(self) -> Mamba2CacheParams:
+        from sglang.srt.layers.dp_attention import get_attention_tp_size
+
+        if _is_cpu:
+            world_size = get_attention_tp_size()
+            adjust_tp_num_heads_if_necessary(self, world_size, False)
+
+        # Qwen3.5 DeltaNet layers are always replicated (attn_tp_size=1)
+        # to avoid FP16 rounding errors accumulating through recurrent state.
+        # The mamba cache dimensions must match the layer's actual TP size (1),
+        # not the model's global TP size.
+        tp_size = 1
+        shape = Mamba2StateShape.create(
+            tp_world_size=tp_size,
+            intermediate_size=self.linear_value_head_dim * self.linear_num_value_heads,
+            n_groups=self.linear_num_key_heads,
+            num_heads=self.linear_num_value_heads,
+            head_dim=self.linear_value_head_dim,
+            state_size=self.linear_key_head_dim,
+            conv_kernel=self.linear_conv_kernel_dim,
+        )
+
+        return Mamba2CacheParams(
+            shape=shape, layers=self.linear_layer_ids, dtype=mamba2_state_dtype(self)
+        )
+
 
 class Qwen3_5Config(PretrainedConfig):
     r"""
diff --git a/python/sglang/srt/layers/quantization/awq.py b/python/sglang/srt/layers/quantization/awq.py
index e85474279..53285c163 100644
--- a/python/sglang/srt/layers/quantization/awq.py
+++ b/python/sglang/srt/layers/quantization/awq.py
@@ -1025,6 +1025,12 @@ class AWQTritonMoEMethod(FusedMoEMethodBase):
         self, layer: torch.nn.Module, moe_runner_config: MoeRunnerConfig
     ):
         self.moe_runner_config = moe_runner_config
+        # Store activation function from model config (Gemma4 uses gelu, Qwen uses silu)
+        act_name = getattr(moe_runner_config, "activation", "silu")
+        if act_name == "gelu":
+            self._act_fn = lambda x: torch.nn.functional.gelu(x, approximate="tanh")
+        else:
+            self._act_fn = torch.nn.functional.silu
 
     def apply(
         self,
@@ -1065,10 +1071,10 @@ class AWQTritonMoEMethod(FusedMoEMethodBase):
             gate_up, sorted_ids, expert_ids_flat, weights_flat,
             top_k, False, 0)
 
-        # Step 2: SiLU activation
+        # Step 2: Activation (gelu for Gemma4, silu for Qwen)
         gate = gate_up[:, :intermediate]
         up = gate_up[:, intermediate:]
-        activated = (torch.nn.functional.silu(gate) * up).contiguous()
+        activated = (self._act_fn(gate) * up).contiguous()
 
         # Step 3: Down GEMV with routing weight applied (top_k=1: each slot is
         # an independent token since each has unique activated features)
@@ -1107,7 +1113,7 @@ class AWQTritonMoEMethod(FusedMoEMethodBase):
 
             gate = gate_up[:, :intermediate]
             up = gate_up[:, intermediate:]
-            activated = torch.nn.functional.silu(gate) * up
+            activated = self._act_fn(gate) * up
 
             w2_fp16 = awq_dequantize_decomposition(
                 layer.w2_qweight[expert_id], layer.w2_scales[expert_id],
diff --git a/python/sglang/srt/models/gemma4_causal.py b/python/sglang/srt/models/gemma4_causal.py
index 32c1dd228..c1776cf62 100644
--- a/python/sglang/srt/models/gemma4_causal.py
+++ b/python/sglang/srt/models/gemma4_causal.py
@@ -904,7 +904,7 @@ class Gemma4ForCausalLM(PreTrainedModel):
             ("gate_up_proj", "up_proj", 1),
         ]
 
-        num_experts = getattr(self.config, "num_experts", 0)
+        num_experts = getattr(self.config, "num_experts", 0) or 0
 
         # Per-expert mapping for AWQ/GPTQ checkpoints (experts.0.gate_proj.qweight)
         per_expert_params_mapping = (
@@ -938,6 +938,13 @@ class Gemma4ForCausalLM(PreTrainedModel):
         for name, loaded_weight in weights:
             name = name.replace("model.language_model.", "model.")
 
+            # CT-GPTQ unfused experts: experts.gate_proj.0. → experts.0.gate_proj.
+            name = re.sub(
+                r"\.experts\.(gate_proj|up_proj|down_proj)\.(\d+)\.",
+                r".experts.\2.\1.",
+                name,
+            )
+
             # HF has router.per_expert_scale and experts.* on the decoder layer;
             # remap into our moe.* subtree since Gemma4MoE owns both.
             name = name.replace(".router.per_expert_scale", ".moe.per_expert_scale")
