diff --git a/python/sglang/kernels/ops/activation/softcap.py b/python/sglang/kernels/ops/activation/softcap.py
index 8b719c9b9..613b007e5 100644
--- a/python/sglang/kernels/ops/activation/softcap.py
+++ b/python/sglang/kernels/ops/activation/softcap.py
@@ -86,10 +86,11 @@ def softcap_inplace_logits_kernel(
     row_ptr = full_logits_ptr + row * row_stride
     x = tl.load(row_ptr + offsets, mask=mask)
 
-    # Perform operations in-place
-    x = x / softcapping_value
-    x = libdevice.tanh(x)
-    x = x * softcapping_value
+    # Perform softcapping in FP32 for precision (BF16 tanh is lossy) — RDNA4 patch 027,
+    # relocated here in v0.5.13 (kernel moved out of logits_processor.py).
+    x_fp32 = x.to(tl.float32) / softcapping_value
+    x_fp32 = libdevice.tanh(x_fp32)
+    x = (x_fp32 * softcapping_value).to(x.dtype)
 
     # Store result
     tl.store(row_ptr + offsets, x, mask=mask)
diff --git a/python/sglang/kernels/ops/attention/decode_attention.py b/python/sglang/kernels/ops/attention/decode_attention.py
index 4d121d573..1099a121a 100644
--- a/python/sglang/kernels/ops/attention/decode_attention.py
+++ b/python/sglang/kernels/ops/attention/decode_attention.py
@@ -31,6 +31,7 @@ import triton.language as tl
 from sglang.kernels.ops.attention.score_mod import unpack_aux_tensors
 from sglang.srt.environ import envs
 from sglang.srt.utils import (
+    get_device_capability,
     get_device_core_count,
     is_gfx95_supported,
     is_gfx1250_supported,
@@ -38,6 +39,7 @@ from sglang.srt.utils import (
 )
 
 _is_hip = is_hip()
+_is_rdna4 = _is_hip and get_device_capability()[0] >= 12
 _is_gfx1250 = _is_hip and is_gfx1250_supported()
 
 logger = logging.getLogger(__name__)
@@ -219,7 +221,8 @@ def _extract_kv_strides(buf, page_size: int):
 @triton.jit
 def tanh(x):
     # Tanh is just a scaled sigmoid
-    return 2 * tl.sigmoid(2 * x) - 1
+    # Compute in FP32 for precision — BF16 tanh is lossy for softcapping
+    return 2 * tl.sigmoid(2 * x.to(tl.float32)) - 1
 
 
 @triton.jit
@@ -430,7 +433,7 @@ def _decode_att_m_fwd(
     BLOCK = 64
     # [TODO] work around SGPR limit on MI3xx
     if _is_hip:
-        BLOCK = 8
+        BLOCK = 32 if _is_rdna4 else 8
     MAX_KV_SPLITS = max_kv_splits
     Lk = k_buffer.shape[-1]
     Lv = v_buffer.shape[-1]
@@ -446,7 +449,7 @@ def _decode_att_m_fwd(
         num_warps = 4
     else:
         num_warps = 2
-        if _is_hip:
+        if _is_hip and not _is_rdna4:
             num_warps = 1
 
     BLOCK_DMODEL = triton.next_power_of_2(Lk)
diff --git a/python/sglang/kernels/ops/attention/extend_attention.py b/python/sglang/kernels/ops/attention/extend_attention.py
index 125d69ba7..80e333137 100644
--- a/python/sglang/kernels/ops/attention/extend_attention.py
+++ b/python/sglang/kernels/ops/attention/extend_attention.py
@@ -214,7 +214,8 @@ def _compact_extend_q_tiles_per_head(
 @triton.jit
 def tanh(x):
     # Tanh is just a scaled sigmoid
-    return 2 * tl.sigmoid(2 * x) - 1
+    # Compute in FP32 for precision — BF16 tanh is lossy for softcapping
+    return 2 * tl.sigmoid(2 * x.to(tl.float32)) - 1
 
 
 @triton.jit
