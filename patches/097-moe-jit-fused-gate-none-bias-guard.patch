diff --git a/python/sglang/srt/layers/moe/topk.py b/python/sglang/srt/layers/moe/topk.py
index 99c8e02ca..9dcb77e76 100644
--- a/python/sglang/srt/layers/moe/topk.py
+++ b/python/sglang/srt/layers/moe/topk.py
@@ -1428,6 +1428,18 @@ def biased_topk_jit_kernel_impl(
         # sqrtsoftplus topk path). moe_fused_gate upcasts the bias to fp32
         # in-register, so pass it through directly rather than allocating a fresh
         # fp32 copy of this static routing bias on every MoE invocation.
+        #
+        # moe_fused_gate requires a per-expert bias tensor. Sigmoid routers
+        # with no correction bias (e.g. Cohere2 / North) pass None, which
+        # faults on `bias.dtype` inside the JIT kernel. Substitute a zero bias
+        # (identity correction), matching the grouped sigmoid JIT path.
+        if correction_bias is None:
+            correction_bias = torch.zeros(
+                gating_output.shape[-1],
+                dtype=torch.float32,
+                device=gating_output.device,
+            )
+
         topk_weights, topk_ids = moe_fused_gate(
             gating_output,
             correction_bias,
