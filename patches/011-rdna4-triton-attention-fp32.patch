diff --git a/python/sglang/srt/layers/attention/triton_ops/decode_attention.py b/python/sglang/srt/layers/attention/triton_ops/decode_attention.py
index 3a57ad090..a05895e64 100644
--- a/python/sglang/srt/layers/attention/triton_ops/decode_attention.py
+++ b/python/sglang/srt/layers/attention/triton_ops/decode_attention.py
@@ -125,7 +125,8 @@ def _fwd_kernel_stage1(
                 mask=(offs_n[:, None] < split_kv_end) & (mask_d[None, :]),
                 other=0.0,
             )
-            qk = tl.sum(q[None, :] * k, 1)
+            # FP32 QK computation to avoid BF16 precision loss over many KV tokens
+            qk = tl.sum(q[None, :].to(tl.float32) * k.to(tl.float32), 1)
             qk *= sm_scale_withk
 
             if logit_cap > 0:
@@ -147,11 +148,12 @@ def _fwd_kernel_stage1(
                 other=0.0,
             )
 
+            # Online softmax in FP32 — BF16 exp/rescaling compounds errors catastrophically
             n_e_max = tl.maximum(tl.max(qk, 0), e_max)
             re_scale = tl.exp(e_max - n_e_max)
             p = tl.exp(qk - n_e_max)
             acc *= re_scale
-            acc += tl.sum(p[:, None] * v, 0)
+            acc += tl.sum(p[:, None] * v.to(tl.float32), 0)
 
             e_sum = e_sum * re_scale + tl.sum(p, 0)
             e_max = n_e_max
@@ -390,11 +392,13 @@ def _fwd_grouped_kernel_stage1(
                 other=0.0,
             )
 
+            # Online softmax in FP32
             n_e_max = tl.maximum(tl.max(qk, 1), e_max)
             re_scale = tl.exp(e_max - n_e_max)
             p = tl.exp(qk - n_e_max[:, None])
             acc *= re_scale[:, None]
-            acc += tl.dot(p.to(v.dtype), v)
+            # FP32 value accumulation — cast v to FP32 to preserve p's FP32 precision
+            acc += tl.dot(p.to(tl.float32), v.to(tl.float32))
 
             e_sum = e_sum * re_scale + tl.sum(p, 1)
             e_max = n_e_max
@@ -562,8 +566,8 @@ def _fwd_kernel_stage2(
         if split_kv_end > split_kv_start:
             tv = tl.load(
                 Mid_O + offs_v + split_kv_id * stride_mid_os, mask=mask_d, other=0.0
-            )
-            tlogic = tl.load(Mid_O_1 + offs_logic + split_kv_id * stride_mid_os // Lv)
+            ).to(tl.float32)
+            tlogic = tl.load(Mid_O_1 + offs_logic + split_kv_id * stride_mid_os // Lv).to(tl.float32)
             n_e_max = tl.maximum(tlogic, e_max)
 
             old_scale = tl.exp(e_max - n_e_max)
diff --git a/python/sglang/srt/layers/attention/triton_ops/extend_attention.py b/python/sglang/srt/layers/attention/triton_ops/extend_attention.py
index d2d97f8c6..267e24b1e 100644
--- a/python/sglang/srt/layers/attention/triton_ops/extend_attention.py
+++ b/python/sglang/srt/layers/attention/triton_ops/extend_attention.py
@@ -61,7 +61,8 @@ def _get_block_sizes_for_extend_attention(Lq: int, Lv: int):
 
     # Determine BLOCK_M, BLOCK_N, and num_warps based on hardware
     if _is_hip:
-        BLOCK_M, BLOCK_N = (64, 64)
+        # RDNA4 has 64KB shared memory — smaller blocks for FP32 value accumulation
+        BLOCK_M, BLOCK_N = (32, 64) if Lq <= 128 else (32, 32)
         num_warps = 4
     else:
         if _is_cuda and CUDA_CAPABILITY[0] == 12:
@@ -376,7 +377,8 @@ def _fwd_kernel(
                 other=0.0,
             )
 
-            qk = tl.dot(q.to(k.dtype), k)
+            # FP32 QK dot product — BF16 accumulation degrades on RDNA4
+            qk = tl.dot(q, k.to(q.dtype))
             if BLOCK_DPE > 0:
                 offs_kpe = (
                     offs_kv_loc[None, :] * stride_buf_kbs
@@ -388,7 +390,7 @@ def _fwd_kernel(
                     mask=mask_n[None, :],
                     other=0.0,
                 )
-                qk += tl.dot(qpe.to(kpe.dtype), kpe)
+                qk += tl.dot(qpe, kpe.to(qpe.dtype))
             qk *= sm_scale * k_scale
 
             if logit_cap > 0:
@@ -399,6 +401,7 @@ def _fwd_kernel(
 
             qk = tl.where(final_mask, qk, float("-inf"))
 
+            # Online softmax in FP32
             row_max = tl.max(qk, 1)
             row_max_fixed = tl.where(row_max == float("-inf"), -1e20, row_max)
             n_e_max = tl.maximum(row_max_fixed, e_max)
@@ -417,8 +420,9 @@ def _fwd_kernel(
                 mask=mask_n[:, None] & mask_dv[None, :],
                 other=0.0,
             )
-            p = p.to(v.dtype)
-            acc = acc * re_scale[:, None] + tl.dot(p, v) * v_scale
+            # FP32 value accumulation — keep p in FP32, cast v to FP32
+            # FP32 value accumulation — prevents BF16 precision loss through 60 layers
+            acc = acc * re_scale[:, None] + tl.dot(p.to(tl.float32), v.to(tl.float32)) * v_scale
 
             e_max = n_e_max
 
@@ -481,7 +485,8 @@ def _fwd_kernel(
                 K_Extend + offs_k, mask=(mask_n[None, :]) & (mask_d[:, None]), other=0.0
             )
 
-            qk = tl.dot(q, k, out_dtype=tl.float32)
+            # FP32 QK dot product
+            qk = tl.dot(q, k.to(q.dtype))
             if BLOCK_DPE > 0:
                 offs_kpe = (
                     (cur_seq_extend_start_idx + start_n + offs_n[None, :]) * stride_kbs
@@ -493,9 +498,9 @@ def _fwd_kernel(
                     mask=mask_n[None, :],
                     other=0.0,
                 )
-                qk += tl.dot(qpe, kpe)
+                qk += tl.dot(qpe, kpe.to(qpe.dtype))
 
-            qk *= sm_scale
+            qk *= sm_scale * k_scale
 
             if logit_cap > 0:
                 qk = logit_cap * tanh(qk / logit_cap)
@@ -505,6 +510,7 @@ def _fwd_kernel(
 
             qk = tl.where(final_mask, qk, float("-inf"))
 
+            # Online softmax in FP32
             row_max = tl.max(qk, 1)
             row_max_fixed = tl.where(row_max == float("-inf"), -1e20, row_max)
             n_e_max = tl.maximum(row_max_fixed, e_max)
@@ -521,8 +527,9 @@ def _fwd_kernel(
             v = tl.load(
                 V_Extend + offs_v, mask=mask_n[:, None] & mask_dv[None, :], other=0.0
             )
-            p = p.to(v.dtype)
-            acc = acc * re_scale[:, None] + tl.dot(p, v)
+            # FP32 value accumulation
+            # FP32 value accumulation
+            acc = acc * re_scale[:, None] + tl.dot(p.to(tl.float32), v.to(tl.float32))
 
             e_max = n_e_max
 
@@ -880,8 +887,8 @@ def _fwd_kernel_unified(
                 other=0.0,
             )
 
-            # Compute QK
-            qk = tl.dot(q.to(k.dtype), k)
+            # FP32 QK dot product — BF16 accumulation degrades on RDNA4
+            qk = tl.dot(q, k.to(q.dtype))
             if BLOCK_DPE > 0:
                 offs_kpe = (
                     offs_kv_loc[None, :] * stride_buf_kbs
@@ -893,7 +900,7 @@ def _fwd_kernel_unified(
                     mask=mask_n[None, :],
                     other=0.0,
                 )
-                qk += tl.dot(qpe.to(kpe.dtype), kpe)
+                qk += tl.dot(qpe, kpe.to(qpe.dtype))
 
             qk *= sm_scale_withk
 
@@ -905,7 +912,7 @@ def _fwd_kernel_unified(
 
             qk = tl.where(final_mask, qk, float("-inf"))
 
-            # Online softmax
+            # Online softmax in FP32
             row_max = tl.max(qk, 1)
             row_max_fixed = tl.where(row_max == float("-inf"), -1e20, row_max)
             n_e_max = tl.maximum(row_max_fixed, e_max)
@@ -925,8 +932,9 @@ def _fwd_kernel_unified(
                 mask=mask_n[:, None] & mask_dv[None, :],
                 other=0.0,
             )
-            p = p.to(v.dtype)
-            acc = acc * re_scale[:, None] + tl.dot(p, v)
+            # FP32 value accumulation
+            # FP32 value accumulation
+            acc = acc * re_scale[:, None] + tl.dot(p.to(tl.float32), v.to(tl.float32))
 
             e_max = n_e_max
 
diff --git a/python/sglang/srt/layers/quantization/gptq.py b/python/sglang/srt/layers/quantization/gptq.py
index 9e5c2945f..f3172d301 100644
--- a/python/sglang/srt/layers/quantization/gptq.py
+++ b/python/sglang/srt/layers/quantization/gptq.py
@@ -67,6 +67,12 @@ if _is_cuda:
     from sgl_kernel import gptq_gemm, gptq_shuffle
 
     from sglang.jit_kernel.gptq_marlin_repack import gptq_marlin_repack
+elif torch.version.hip is not None:
+    # HIP/ROCm: gptq_gemm and gptq_shuffle are available in our sgl_kernel build
+    try:
+        from sgl_kernel import gptq_gemm, gptq_shuffle
+    except ImportError:
+        pass
 
 _is_npu = is_npu()
 
@@ -565,6 +571,11 @@ class GPTQLinearMethod(LinearMethodBase):
         layer.g_idx = torch.nn.Parameter(layer.g_idx.data, requires_grad=False)
         layer.scales = torch.nn.Parameter(layer.scales.data, requires_grad=False)
 
+        # HIP/ROCm: skip shuffle, use torch dequant fallback
+        if torch.version.hip is not None:
+            self._use_hip_fallback = True
+            return
+
         # exllama needs to shuffle the weight after the weight is loaded
         # here we do the shuffle on first forward pass
         if self.use_shuffle:
@@ -576,6 +587,56 @@ class GPTQLinearMethod(LinearMethodBase):
                 )
             gptq_shuffle(layer.qweight, layer.g_idx, self.quant_config.weight_bits)
 
+    @staticmethod
+    def _hip_gptq_dequant_matmul(
+        x: torch.Tensor,
+        qweight: torch.Tensor,
+        qzeros: torch.Tensor,
+        scales: torch.Tensor,
+        g_idx: torch.Tensor,
+        bits: int,
+    ) -> torch.Tensor:
+        """Torch-based GPTQ dequant+matmul for HIP/ROCm."""
+        pack_factor = 32 // bits
+        # qweight: [K//pack, N] int32, GPTQ sequential packing
+        K = qweight.shape[0] * pack_factor
+        N = qweight.shape[1]
+
+        # Unpack int32 → 4-bit values
+        # qweight is [K//pack, N] with pack along K dim
+        unpacked = []
+        for i in range(pack_factor):
+            unpacked.append((qweight >> (i * bits)) & ((1 << bits) - 1))
+        # Stack along dim=1 (between K/pack and N) to get [K/pack, pack, N]
+        # then reshape to [K, N] so K indices are contiguous
+        w_int = torch.stack(unpacked, dim=1).reshape(K, N)  # [K, N]
+
+        # Apply g_idx reordering if present
+        if g_idx.numel() > 0 and g_idx.numel() == K:
+            group_indices = g_idx
+        else:
+            group_size = K // scales.shape[0] if scales.shape[0] > 0 else K
+            group_indices = torch.arange(K, device=qweight.device) // group_size
+
+        # Dequantize: (w_int - zp) * scale
+        # qzeros: [groups, N//pack] int32
+        zp_unpacked = []
+        for i in range(pack_factor):
+            zp_unpacked.append((qzeros >> (i * bits)) & ((1 << bits) - 1))
+        zp_all = torch.stack(zp_unpacked, dim=-1).reshape(qzeros.shape[0], -1)  # [groups, N]
+        if zp_all.shape[1] > N:
+            zp_all = zp_all[:, :N]
+
+        # Expand per group
+        zp = zp_all[group_indices]  # [K, N]
+        sc = scales[group_indices]  # [K, N]
+
+        # GPTQ v1 format: actual_zp = stored_zp + 1
+        w_float = (w_int.to(torch.float32) - (zp.to(torch.float32) + 1.0)) * sc.to(torch.float32)
+
+        # Matmul in FP32 to avoid BF16 precision loss through 60 layers
+        return torch.matmul(x.to(torch.float32), w_float).to(x.dtype)
+
     def apply(
         self,
         layer: torch.nn.Module,
@@ -585,15 +646,25 @@ class GPTQLinearMethod(LinearMethodBase):
         out_shape = x.shape[:-1] + (layer.qweight.shape[-1],)
         reshaped_x = x.reshape(-1, x.shape[-1])
 
-        output = gptq_gemm(
-            reshaped_x,
-            layer.qweight,
-            layer.qzeros,
-            layer.scales,
-            layer.g_idx,
-            self.use_shuffle,
-            self.quant_config.weight_bits,
-        )
+        if getattr(self, '_use_hip_fallback', False):
+            output = self._hip_gptq_dequant_matmul(
+                reshaped_x,
+                layer.qweight,
+                layer.qzeros,
+                layer.scales,
+                layer.g_idx,
+                self.quant_config.weight_bits,
+            )
+        else:
+            output = gptq_gemm(
+                reshaped_x,
+                layer.qweight,
+                layer.qzeros,
+                layer.scales,
+                layer.g_idx,
+                self.use_shuffle,
+                self.quant_config.weight_bits,
+            )
         if bias is not None:
             output.add_(bias)
         return output.reshape(out_shape)
diff --git a/python/sglang/srt/layers/quantization/marlin_utils.py b/python/sglang/srt/layers/quantization/marlin_utils.py
index 04b72ca0d..3e06b4861 100644
--- a/python/sglang/srt/layers/quantization/marlin_utils.py
+++ b/python/sglang/srt/layers/quantization/marlin_utils.py
@@ -119,6 +119,10 @@ def _check_marlin_supported(
     device_capability: Optional[int] = None,
 ) -> tuple[bool, Optional[str]]:
 
+    # Marlin kernels are CUDA-only; disable on HIP/ROCm
+    if torch.version.hip is not None:
+        return False, "Marlin is not supported on HIP/ROCm"
+
     if device_capability is None:
         major, minor = get_device_capability()
         capability = major * 10 + minor
