diff --git a/python/sglang/srt/models/glm4_moe.py b/python/sglang/srt/models/glm4_moe.py
index 9ee560114c..e94838eef6 100644
--- a/python/sglang/srt/models/glm4_moe.py
+++ b/python/sglang/srt/models/glm4_moe.py
@@ -896,6 +896,11 @@ class Glm4MoeDecoderLayer(nn.Module):
             is_last_layer=(
                 is_nextn or (self.layer_id == self.config.num_hidden_layers - 1)
             ),
+            # GLM-4.5 MoE is a conventional attention model (no recurrent state), so opt
+            # out of the universal HIP FP32 attention all-reduce and use the BF16
+            # collective. Same switch as patches 080/084; FP32 stays the default for
+            # recurrent DeltaNet/Mamba users. A/B on 2x R9700: +1.3-4.1%, coherent.
+            hip_fp32_allreduce=False,
         )
 
         # Detect if QKV uses aiter FP8 per-token quant so we can fuse
