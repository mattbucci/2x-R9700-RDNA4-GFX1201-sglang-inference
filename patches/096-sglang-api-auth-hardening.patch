diff --git a/python/sglang/srt/arg_groups/arg_utils.py b/python/sglang/srt/arg_groups/arg_utils.py
index 0c54e930f..187c713a2 100644
--- a/python/sglang/srt/arg_groups/arg_utils.py
+++ b/python/sglang/srt/arg_groups/arg_utils.py
@@ -90,6 +90,11 @@ class Arg(msgspec.Struct, frozen=True):
     # that depends on the machine, on another field, or on anything impure is a
     # decision, and decisions stay in a hook where their order is visible.
     fallback: Any = None
+    # When True, the field carries a credential (API keys, key passwords).
+    # Whole-record readbacks (`/server_info`, `get_internal_state`, request
+    # dumps, startup logs) drop it via `sensitive_fields`, and the record's
+    # repr masks it.
+    sensitive: bool = False
 
 
 class Derived(msgspec.Struct, frozen=True):
@@ -205,6 +210,21 @@ def resolvable_fields(cls) -> frozenset:
     return frozenset(names)
 
 
+@functools.cache
+def sensitive_fields(cls) -> frozenset:
+    """Names of ``cls`` record fields whose ``Arg`` metadata declares
+    ``sensitive=True`` -- credentials that must not appear in a readback."""
+    if not is_record(cls):
+        return frozenset()
+    hints = get_type_hints(cls, include_extras=True)
+    names = set()
+    for field in record_fields(cls):
+        _, arg = _unwrap_annotated(hints.get(field.name, field.type))
+        if arg is not None and arg.sensitive:
+            names.add(field.name)
+    return frozenset(names)
+
+
 @functools.cache
 def fallbacks_of(cls) -> dict:
     """``{field_name: value}`` for every field of ``cls`` that declares one.
diff --git a/python/sglang/srt/arg_groups/fields/serving.py b/python/sglang/srt/arg_groups/fields/serving.py
index a85a3220f..2a412442e 100644
--- a/python/sglang/srt/arg_groups/fields/serving.py
+++ b/python/sglang/srt/arg_groups/fields/serving.py
@@ -142,7 +142,7 @@ class Serving(msgspec.Struct):
     ssl_ca_certs: A[Optional[str], "The CA certificates file."] = None
     ssl_keyfile_password: A[
         Optional[str],
-        "The password to decrypt the SSL keyfile.",
+        Arg(help="The password to decrypt the SSL keyfile.", sensitive=True),
     ] = None
     enable_ssl_refresh: A[
         bool,
@@ -154,11 +154,17 @@ class Serving(msgspec.Struct):
     # -------------------------------------------------------------------------
     api_key: A[
         Optional[str],
-        "Set API key of the server. It is also used in the OpenAI API compatible server.",
+        Arg(
+            help="Set API key of the server. It is also used in the OpenAI API compatible server.",
+            sensitive=True,
+        ),
     ] = None
     admin_api_key: A[
         Optional[str],
-        "Set admin API key for sensitive management endpoints (e.g. /clear_hicache_storage_backend). When set, admin endpoints require this key and do NOT accept --api-key.",
+        Arg(
+            help="Set admin API key for sensitive management endpoints (e.g. /clear_hicache_storage_backend). When set, admin endpoints require this key and do NOT accept --api-key.",
+            sensitive=True,
+        ),
     ] = None
     served_model_name: A[
         Optional[str],
diff --git a/python/sglang/srt/distributed/bootstrap.py b/python/sglang/srt/distributed/bootstrap.py
index 0d87af677..6ffd52b7d 100644
--- a/python/sglang/srt/distributed/bootstrap.py
+++ b/python/sglang/srt/distributed/bootstrap.py
@@ -191,9 +191,12 @@ def _resolve_dist_init_method(*, dist_port: int) -> str:
         na = NetworkAddress.parse(get_parallel().dist_init_addr)
         dist_init_method = na.to_tcp()
     else:
-        dist_init_method = NetworkAddress(
-            get_serving().host or "127.0.0.1", dist_port
-        ).to_tcp()
+        bootstrap_host = (
+            "127.0.0.1"
+            if get_parallel().nnodes == 1
+            else (get_serving().host or "127.0.0.1")
+        )
+        dist_init_method = NetworkAddress(bootstrap_host, dist_port).to_tcp()
     return dist_init_method
 
 
diff --git a/python/sglang/srt/entrypoints/engine.py b/python/sglang/srt/entrypoints/engine.py
index 9afd8c480..81ab37d79 100644
--- a/python/sglang/srt/entrypoints/engine.py
+++ b/python/sglang/srt/entrypoints/engine.py
@@ -116,7 +116,11 @@ from sglang.srt.runtime_context import (
     restore_context,
     snapshot_context,
 )
-from sglang.srt.server_args import PortArgs, ServerArgs
+from sglang.srt.server_args import (
+    PortArgs,
+    ServerArgs,
+    server_args_to_public_dict,
+)
 from sglang.srt.utils import (
     MultiprocessingSerializer,
     SerializedTensorPayload,
@@ -273,7 +277,7 @@ class Engine(EngineScoreMixin, EngineBase):
                 "Engine(" + ", ".join(f"{k}={v!r}" for k, v in kwargs.items()) + ")",
             )
         self.server_args = server_args
-        logger.info(f"server_args={server_args.resolved_dict()}")
+        logger.info("server_args=%s", server_args_to_public_dict(server_args))
 
         # Rust Server is not supported with the offline Engine API
         if envs.SGLANG_RUST_SERVER.get():
@@ -1106,7 +1110,7 @@ class Engine(EngineScoreMixin, EngineBase):
             # Allocate ports for inter-process communications
             if port_args is None:
                 port_args = PortArgs.init_new(server_args)
-            logger.info(f"server_args={server_args.resolved_dict()}")
+            logger.info("server_args=%s", server_args_to_public_dict(server_args))
 
             # Start the engine info bootstrap server if per-rank info is needed.
             engine_info_bootstrap_server = None
@@ -1379,7 +1383,7 @@ class Engine(EngineScoreMixin, EngineBase):
         )
         return msgspec_to_builtins(
             {
-                **self.tokenizer_manager.server_args.resolved_dict(),
+                **server_args_to_public_dict(self.tokenizer_manager.server_args),
                 "launch_command": self.tokenizer_manager.server_args.launch_command,
                 **self._scheduler_init_result.scheduler_infos[0],
                 "startup_time": self.tokenizer_manager.startup_time,
diff --git a/python/sglang/srt/entrypoints/grpc_bridge.py b/python/sglang/srt/entrypoints/grpc_bridge.py
index 234ab8bb7..a8f2fc31d 100644
--- a/python/sglang/srt/entrypoints/grpc_bridge.py
+++ b/python/sglang/srt/entrypoints/grpc_bridge.py
@@ -16,6 +16,7 @@ from pydantic import ValidationError
 
 from sglang.srt.arg_groups.overrides import resolving_view
 from sglang.srt.configs.embedding_model_spec import resolved_embedding_plan
+from sglang.srt.server_args import server_args_to_public_dict
 from sglang.srt.runtime_context import (
     describe_kv_events_publisher,
     get_lora,
@@ -427,7 +428,9 @@ class RuntimeHandle:
         return json.dumps(result, default=str)
 
     def get_server_info(self) -> str:
-        result: Dict[str, Any] = self.tokenizer_manager.server_args.resolved_dict()
+        result: Dict[str, Any] = server_args_to_public_dict(
+            self.tokenizer_manager.server_args
+        )
         # `resolved_dict` answers with what resolution decided; the launch
         # command answers with what was asked for, and the two are not
         # derivable from each other. The HTTP and in-process readbacks both
diff --git a/python/sglang/srt/entrypoints/http_server.py b/python/sglang/srt/entrypoints/http_server.py
index f51d4399b..962977dd8 100644
--- a/python/sglang/srt/entrypoints/http_server.py
+++ b/python/sglang/srt/entrypoints/http_server.py
@@ -165,7 +165,11 @@ from sglang.srt.observability.trace import (
 )
 from sglang.srt.parser.reasoning_parser import ReasoningParser
 from sglang.srt.parser.template_manager import TemplateManager
-from sglang.srt.server_args import PortArgs, ServerArgs
+from sglang.srt.server_args import (
+    PortArgs,
+    ServerArgs,
+    server_args_to_public_dict,
+)
 from sglang.srt.utils import (
     add_prometheus_middleware,
     add_prometheus_track_response_middleware,
@@ -806,6 +810,7 @@ async def weight_version():
 
 
 @app.get("/get_server_info")
+@auth_level(AuthLevel.ADMIN_FORCE)
 async def get_server_info():
     """Get the server information (deprecated - use /server_info instead)."""
     logger.warning(
@@ -816,6 +821,7 @@ async def get_server_info():
 
 
 @app.get("/server_info")
+@auth_level(AuthLevel.ADMIN_FORCE)
 async def server_info():
     """The startup configuration, plus live scheduler state.
 
@@ -836,7 +842,7 @@ async def server_info():
 
     return msgspec_to_builtins(
         {
-            **server_args.resolved_dict(),
+            **server_args_to_public_dict(server_args),
             "launch_command": server_args.launch_command,
             **_global_state.scheduler_info,
             "startup_time": _global_state.tokenizer_manager.startup_time,
@@ -1573,6 +1579,7 @@ async def load_lora_adapter(
 
 
 @app.api_route("/load_lora_adapter_from_tensors", methods=["POST"])
+@auth_level(AuthLevel.ADMIN_OPTIONAL)
 async def load_lora_adapter_from_tensors(
     obj: Annotated[LoadLoRAAdapterFromTensorsReqInput, Body()], request: Request
 ):
diff --git a/python/sglang/srt/managers/scheduler.py b/python/sglang/srt/managers/scheduler.py
index 743c8afe0..c670e2f23 100644
--- a/python/sglang/srt/managers/scheduler.py
+++ b/python/sglang/srt/managers/scheduler.py
@@ -313,7 +313,12 @@ from sglang.srt.platforms import current_platform
 from sglang.srt.plugins import load_plugins
 from sglang.srt.rust_server.server import RustServer
 from sglang.srt.sampling.sampling_batch_info import SamplingBatchInfo
-from sglang.srt.server_args import PortArgs, ServerArgs, compute_world_size
+from sglang.srt.server_args import (
+    PortArgs,
+    ServerArgs,
+    compute_world_size,
+    redact_server_args_dict,
+)
 from sglang.srt.session.session_controller import SessionController
 from sglang.srt.speculative.base_spec_worker import BaseSpecWorker
 from sglang.srt.speculative.dflash_utils import validate_dflash_request
@@ -5012,7 +5017,7 @@ class Scheduler(
     def get_internal_state(self, recv_req: GetInternalStateReq):
         # Resolved config (pristine server_args + post-publish overrides) so a
         # readback reflects values changed via /set_internal_state, not startup.
-        ret = get_context().resolved_server_args_dict()
+        ret = redact_server_args_dict(get_context().resolved_server_args_dict())
         ret["world_size"] = compute_world_size(
             enable_dp_attention=get_parallel().enable_dp_attention,
             dp_size=get_parallel().dp_size,
diff --git a/python/sglang/srt/managers/tokenizer_manager.py b/python/sglang/srt/managers/tokenizer_manager.py
index 1b63fb642..fa33218a9 100644
--- a/python/sglang/srt/managers/tokenizer_manager.py
+++ b/python/sglang/srt/managers/tokenizer_manager.py
@@ -158,6 +158,9 @@ from sglang.srt.sampling.sampling_params import SamplingParams
 from sglang.srt.server_args import (
     PortArgs,
     ServerArgs,
+    redact_sensitive_cli_args,
+    redact_server_args_dict,
+    server_args_to_public_dict,
 )
 from sglang.srt.utils import (
     configure_gc_warning,
@@ -2195,7 +2198,9 @@ class TokenizerManager(TokenizerControlMixin, TokenizerManagerScoreMixin):
         part that cannot be reconstructed afterwards.
         """
         try:
-            return self.resolved_config_dict(self.server_args.resolved_dict())
+            return redact_server_args_dict(
+                self.resolved_config_dict(self.server_args.resolved_dict())
+            )
         except Exception as e:
             logger.error(f"Failed to snapshot the resolved config for the dump: {e!r}")
             return None
@@ -3138,7 +3143,7 @@ class TokenizerManager(TokenizerControlMixin, TokenizerManagerScoreMixin):
     ):
         logger.info(log_message)
         to_dump_with_server_args = {
-            "server_args": self.server_args,
+            "server_args": server_args_to_public_dict(self.server_args),
             "config_updates": get_context().overrides_log(),
             "resolved_config": self._dump_config_snapshot(),
             "requests": data_list.copy(),
@@ -3223,11 +3228,11 @@ class TokenizerManager(TokenizerControlMixin, TokenizerManagerScoreMixin):
 
                 # Write the data to the file
                 data_to_dump_with_server_args = {
-                    "server_args": self.server_args,
+                    "server_args": server_args_to_public_dict(self.server_args),
                     "config_updates": get_context().overrides_log(),
                     "resolved_config": self._dump_config_snapshot(),
                     "requests": data_to_dump,
-                    "launch_command": " ".join(sys.argv),
+                    "launch_command": " ".join(redact_sensitive_cli_args(sys.argv)),
                 }
                 with open(filename, "wb") as f:
                     try:
diff --git a/python/sglang/srt/multimodal/processors/mimo_audio.py b/python/sglang/srt/multimodal/processors/mimo_audio.py
index 909c9965b..7d123657d 100644
--- a/python/sglang/srt/multimodal/processors/mimo_audio.py
+++ b/python/sglang/srt/multimodal/processors/mimo_audio.py
@@ -178,11 +178,15 @@ class MiMoAudioPipeline:
             if isinstance(audio, bytes):
                 file = io.BytesIO(audio)
             elif isinstance(audio, str):
-                if audio.startswith("data:"):
+                scheme = common._media_source_scheme(audio)
+                if scheme == "data":
                     file = io.BytesIO(
-                        pybase64.b64decode(audio.split(",")[1], validate=True)
+                        pybase64.b64decode(audio.split(",", 1)[1], validate=True)
+                    )
+                elif scheme in {"http", "https"}:
+                    common._require_media_source_enabled(
+                        "SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL"
                     )
-                elif audio.startswith("http://") or audio.startswith("https://"):
                     dl_start = time.perf_counter()
                     timeout = int(os.getenv("REQUEST_TIMEOUT", "5"))
                     try:
@@ -201,7 +205,17 @@ class MiMoAudioPipeline:
                             f"error={type(e).__name__}: {e}, url={audio}"
                         )
                         raise
+                elif scheme == "file":
+                    common._require_media_source_enabled(
+                        "SGLANG_ALLOW_LOCAL_MEDIA", "Local file"
+                    )
+                    file = common.unquote(common.urlparse(audio).path)
+                elif scheme:
+                    common._reject_unsupported_media_scheme(audio, scheme)
                 else:
+                    common._require_media_source_enabled(
+                        "SGLANG_ALLOW_LOCAL_MEDIA", "Local file"
+                    )
                     file = audio
             if AudioDecoder is None:
                 raise RuntimeError(
diff --git a/python/sglang/srt/multimodal/processors/mimo_v2.py b/python/sglang/srt/multimodal/processors/mimo_v2.py
index c833d7937..9c431416a 100644
--- a/python/sglang/srt/multimodal/processors/mimo_v2.py
+++ b/python/sglang/srt/multimodal/processors/mimo_v2.py
@@ -490,22 +490,41 @@ class MiMoProcessor:
         # Never hand a client-supplied URL to ffprobe: its internal HTTP client
         # would bypass the shared domain and redirect policy. Resolve it through
         # the guarded downloader first, then probe the resulting bytes in-process.
-        if isinstance(path_or_data, str) and path_or_data.startswith(
-            ("http://", "https://")
-        ):
+        if isinstance(path_or_data, str):
+            from sglang.srt.utils.common import (
+                _media_source_scheme,
+                _reject_unsupported_media_scheme,
+                _require_media_source_enabled,
+            )
+
+            scheme = _media_source_scheme(path_or_data)
+        else:
+            scheme = ""
+        if scheme in {"http", "https"}:
+            _require_media_source_enabled("SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL")
             timeout = int(os.getenv("REQUEST_TIMEOUT", "10"))
             path_or_data = download_remote_media(path_or_data, timeout=timeout)
+        elif scheme and scheme not in {"data", "file"}:
+            _reject_unsupported_media_scheme(path_or_data, scheme)
 
         if isinstance(path_or_data, bytes):
             source = BytesIO(path_or_data)
         elif (
             isinstance(path_or_data, str)
-            and path_or_data.startswith("data:")
+            and scheme == "data"
             and ";base64," in path_or_data
         ):
             source = BytesIO(base64.b64decode(path_or_data.split(";base64,")[1]))
         else:
-            source = path_or_data  # local path or file://
+            if isinstance(path_or_data, str):
+                _require_media_source_enabled(
+                    "SGLANG_ALLOW_LOCAL_MEDIA", "Local file"
+                )
+            source = (
+                path_or_data[7:]
+                if isinstance(path_or_data, str) and scheme == "file"
+                else path_or_data
+            )
         try:
             AudioDecoder(source)
             return True
@@ -1449,18 +1468,36 @@ class MiMoProcessor:
         if isinstance(image, Image.Image):
             image_obj = image
         elif isinstance(image, str):
-            if image.startswith("http://") or image.startswith("https://"):
+            from sglang.srt.utils.common import (
+                _media_source_scheme,
+                _reject_unsupported_media_scheme,
+                _require_media_source_enabled,
+            )
+
+            scheme = _media_source_scheme(image)
+            if scheme in {"http", "https"}:
+                _require_media_source_enabled(
+                    "SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL"
+                )
                 with BytesIO(download_remote_media(image, timeout=3)) as bio:
                     image_obj = copy.deepcopy(Image.open(bio))
-            elif image.startswith("file://"):
+            elif scheme == "file":
+                _require_media_source_enabled(
+                    "SGLANG_ALLOW_LOCAL_MEDIA", "Local file"
+                )
                 image_obj = Image.open(image[7:])
-            elif image.startswith("data:image"):
+            elif scheme == "data":
                 if "base64," in image:
                     _, base64_data = image.split("base64,", 1)
                     data = base64.b64decode(base64_data)
                     with BytesIO(data) as bio:
                         image_obj = copy.deepcopy(Image.open(bio))
+            elif scheme:
+                _reject_unsupported_media_scheme(image, scheme)
             else:
+                _require_media_source_enabled(
+                    "SGLANG_ALLOW_LOCAL_MEDIA", "Local file"
+                )
                 image_obj = Image.open(image)
         else:
             image_obj = Image.open(BytesIO(image))
diff --git a/python/sglang/srt/multimodal/processors/moss_vl.py b/python/sglang/srt/multimodal/processors/moss_vl.py
index 3f862f355..24232a3a6 100644
--- a/python/sglang/srt/multimodal/processors/moss_vl.py
+++ b/python/sglang/srt/multimodal/processors/moss_vl.py
@@ -20,7 +20,12 @@ from sglang.srt.multimodal.processors.base_processor import (
 from sglang.srt.multimodal.processors.base_processor import (
     MultimodalSpecialTokens,
 )
-from sglang.srt.utils.common import download_remote_media
+from sglang.srt.utils.common import (
+    _media_source_scheme,
+    _reject_unsupported_media_scheme,
+    _require_media_source_enabled,
+    download_remote_media,
+)
 
 
 class MossVLImageProcessor(SGLangBaseProcessor):
@@ -425,20 +430,24 @@ class MossVLImageProcessor(SGLangBaseProcessor):
             raise
 
     def _normalize_video_string(self, value: str) -> Tuple[str, Optional[str]]:
-        if value.startswith("file://"):
+        scheme = _media_source_scheme(value)
+        if scheme == "file":
+            _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
             return self._resolve_file_url(value), None
 
-        if os.path.isfile(value):
+        if not scheme and os.path.isfile(value):
+            _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
             return value, None
 
-        if value.startswith(("http://", "https://")):
+        if scheme in {"http", "https"}:
+            _require_media_source_enabled("SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL")
             timeout = int(os.getenv("REQUEST_TIMEOUT", "10"))
             content = download_remote_media(value, timeout=timeout)
             suffix = os.path.splitext(urlparse(value).path)[1] or ".mp4"
             temp_path = self._write_video_bytes_to_tempfile(content, suffix=suffix)
             return temp_path, temp_path
 
-        if value.startswith("data:"):
+        if scheme == "data":
             header, encoded = value.split(",", 1)
             mime = header.split(";", 1)[0]
             suffix = ".mp4"
@@ -452,6 +461,9 @@ class MossVLImageProcessor(SGLangBaseProcessor):
             )
             return temp_path, temp_path
 
+        if scheme:
+            _reject_unsupported_media_scheme(value, scheme)
+
         temp_path = self._write_video_bytes_to_tempfile(
             pybase64.b64decode(value, validate=True)
         )
diff --git a/python/sglang/srt/multimodal/processors/transformers_auto.py b/python/sglang/srt/multimodal/processors/transformers_auto.py
index 17f486ddf..f73adde86 100644
--- a/python/sglang/srt/multimodal/processors/transformers_auto.py
+++ b/python/sglang/srt/multimodal/processors/transformers_auto.py
@@ -12,6 +12,7 @@ from sglang.srt.multimodal.processors.base_processor import (
     MultimodalSpecialTokens,
 )
 from sglang.srt.utils import load_image
+from sglang.srt.utils.common import _enforce_delegated_media_source_policy
 
 
 def _first_attr(obj, names: tuple[str, ...], default=None):
@@ -186,6 +187,8 @@ class TransformersAutoMultimodalProcessor(BaseMultimodalProcessor):
         video_data = getattr(request_obj, "video_data", None)
         if video_data is not None and not isinstance(video_data, list):
             video_data = [video_data]
+        for video in video_data or []:
+            _enforce_delegated_media_source_policy(video)
 
         # Audio path: load clips and run the shared token-expansion machinery,
         # which passes ``audio=`` to the HF processor and packs audio features
diff --git a/python/sglang/srt/server_args.py b/python/sglang/srt/server_args.py
index d79bb75d7..f529b3c17 100644
--- a/python/sglang/srt/server_args.py
+++ b/python/sglang/srt/server_args.py
@@ -49,6 +49,7 @@ from sglang.srt.arg_groups.arg_utils import (
     add_cli_args_from_dataclass,
     is_record,
     record_fields,
+    sensitive_fields,
 )
 from sglang.srt.arg_groups.argparse_actions import (
     DeprecatedStoreTrueAction,
@@ -308,6 +309,18 @@ class ServerArgs:
         """
         return getattr(self, "_launch_command", None)
 
+    def __repr__(self) -> str:
+        # The assembled Struct's default repr prints every field; credentials
+        # (`Arg(sensitive=True)`) are masked so a stray log of the record
+        # cannot leak them.
+        hidden = sensitive_fields(type(self))
+        parts = ", ".join(
+            f"{field.name}="
+            + ("<redacted>" if field.name in hidden else repr(getattr(self, field.name)))
+            for field in record_fields(type(self))
+        )
+        return f"ServerArgs({parts})"
+
     def resolved_dict(self) -> dict[str, Any]:
         """This configuration as a plain dict of resolved field values.
 
@@ -626,6 +639,47 @@ def _underscore_field_names() -> frozenset:
     )
 
 
+def _sensitive_server_arg_fields() -> frozenset[str]:
+    return sensitive_fields(ServerArgs)
+
+
+def redact_server_args_dict(values: dict[str, Any]) -> dict[str, Any]:
+    """Copy a ServerArgs-shaped mapping without credential-bearing fields."""
+    values = dict(values)
+    for field_name in _sensitive_server_arg_fields():
+        values.pop(field_name, None)
+    return values
+
+
+def server_args_to_public_dict(server_args: Any) -> dict[str, Any]:
+    """Serialize ServerArgs (resolved values) without credential-bearing fields."""
+    return redact_server_args_dict(server_args.resolved_dict())
+
+
+def redact_sensitive_cli_args(argv: list[str]) -> list[str]:
+    """Return argv with values for credential-bearing ServerArgs removed."""
+    sensitive_options = {
+        f"--{name.replace('_', '-')}" for name in _sensitive_server_arg_fields()
+    }
+    result: list[str] = []
+    redact_next = False
+    for arg in argv:
+        if redact_next:
+            result.append("<redacted>")
+            redact_next = False
+            continue
+        matching_option = next(
+            (option for option in sensitive_options if arg.startswith(f"{option}=")),
+            None,
+        )
+        if matching_option:
+            result.append(f"{matching_option}=<redacted>")
+        else:
+            result.append(arg)
+            redact_next = arg in sensitive_options
+    return result
+
+
 # NOTE: The process-wide ServerArgs is owned by the runtime context
 # (sglang.srt.runtime_context). The two publish functions below are LEGACY
 # shims kept for the existing call-sites; they hand over the same live object
diff --git a/python/sglang/srt/utils/auth.py b/python/sglang/srt/utils/auth.py
index df6811179..08ce4f0a5 100644
--- a/python/sglang/srt/utils/auth.py
+++ b/python/sglang/srt/utils/auth.py
@@ -172,6 +172,26 @@ def add_api_key_middleware(
             self.fastapi_app = fastapi_app
 
         async def __call__(self, scope, receive, send):
+            if scope["type"] == "websocket":
+                authz = None
+                for name, value in scope.get("headers", []):
+                    if name.lower() == b"authorization":
+                        authz = value.decode("latin-1")
+                        break
+                decision = decide_request_auth(
+                    method="GET",
+                    path=scope.get("path", ""),
+                    authorization_header=authz,
+                    api_key=self.api_key or self.admin_api_key,
+                    admin_api_key=None,
+                    auth_level=AuthLevel.NORMAL,
+                )
+                if not decision.allowed:
+                    await send({"type": "websocket.close", "code": 1008})
+                    return
+                await self.app(scope, receive, send)
+                return
+
             if scope["type"] != "http":
                 await self.app(scope, receive, send)
                 return
diff --git a/python/sglang/srt/utils/common.py b/python/sglang/srt/utils/common.py
index 62ad5159f..d30ce5580 100644
--- a/python/sglang/srt/utils/common.py
+++ b/python/sglang/srt/utils/common.py
@@ -1704,6 +1704,64 @@ CLIENT_MEDIA_EXCEPTIONS = (
 )
 
 
+def _require_media_source_enabled(env_name: str, source_kind: str) -> None:
+    if os.getenv(env_name, "1").strip().lower() not in {"1", "true", "yes"}:
+        raise ValueError(f"{source_kind} multimodal inputs are disabled")
+
+
+def _media_source_scheme(value: str) -> str:
+    return urlparse(value.strip()).scheme.lower()
+
+
+def _reject_unsupported_media_scheme(value: str, scheme: str) -> None:
+    _require_media_source_enabled("SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL")
+    raise ValueError(f"Unsupported multimodal URL scheme: {scheme!r}")
+
+
+def _enforce_delegated_media_source_policy(value: Any) -> None:
+    """Guard sources passed to third-party processors that may perform their own I/O."""
+    if isinstance(value, (ImageData, VideoData)):
+        value = value.url
+    if isinstance(value, dict):
+        source_keys = {
+            "url",
+            "path",
+            "uri",
+            "src",
+            "source",
+            "image",
+            "video",
+            "audio",
+            "image_url",
+            "video_url",
+            "audio_url",
+            "image_path",
+            "video_path",
+            "audio_path",
+        }
+        for key, item in value.items():
+            if str(key).lower() in source_keys or isinstance(item, (dict, list, tuple)):
+                _enforce_delegated_media_source_policy(item)
+        return
+    if isinstance(value, (list, tuple)):
+        for item in value:
+            _enforce_delegated_media_source_policy(item)
+        return
+    if not isinstance(value, str):
+        return
+    scheme = _media_source_scheme(value)
+    if scheme == "data":
+        return
+    if scheme in {"http", "https"}:
+        _require_media_source_enabled("SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL")
+    elif scheme and scheme != "file":
+        _reject_unsupported_media_scheme(value, scheme)
+    else:
+        # A delegated processor may interpret any other string as a relative,
+        # absolute, or file:// path. Only explicit data: URIs bypass this gate.
+        _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
+
+
 def load_audio(
     audio_file: Union[str, bytes], sr: Optional[int] = None, mono: bool = True
 ) -> np.ndarray:
@@ -1713,17 +1771,22 @@ def load_audio(
     # Normalize input: resolve URL / base64 / file:// to bytes or path
     if isinstance(audio_file, bytes):
         source = audio_file
-    elif isinstance(audio_file, str) and audio_file.startswith("data:"):
-        source = pybase64.b64decode(audio_file.split(",")[1], validate=True)
-    elif isinstance(audio_file, str) and (
-        audio_file.startswith("http://") or audio_file.startswith("https://")
-    ):
-        timeout = int(os.getenv("REQUEST_TIMEOUT", "5"))
-        source = download_remote_media(audio_file, timeout=timeout)
-    elif isinstance(audio_file, str) and audio_file.startswith("file://"):
-        source = unquote(urlparse(audio_file).path)
     elif isinstance(audio_file, str):
-        source = audio_file
+        scheme = _media_source_scheme(audio_file)
+        if scheme == "data":
+            source = pybase64.b64decode(audio_file.split(",", 1)[1], validate=True)
+        elif scheme in {"http", "https"}:
+            _require_media_source_enabled("SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL")
+            timeout = int(os.getenv("REQUEST_TIMEOUT", "5"))
+            source = download_remote_media(audio_file, timeout=timeout)
+        elif scheme == "file":
+            _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
+            source = unquote(urlparse(audio_file).path)
+        elif scheme:
+            _reject_unsupported_media_scheme(audio_file, scheme)
+        else:
+            _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
+            source = audio_file
     else:
         raise ValueError(f"Invalid audio format: {audio_file}")
 
@@ -1982,18 +2045,27 @@ def get_image_bytes(image_file: Union[str, bytes]) -> bytes:
     """Normalize various image inputs into raw bytes."""
     if isinstance(image_file, bytes):
         return image_file
-    if image_file.startswith(("http://", "https://")):
+    if not isinstance(image_file, str):
+        raise NotImplementedError(f"Invalid image: {image_file}")
+    scheme = _media_source_scheme(image_file)
+    if scheme in {"http", "https"}:
+        _require_media_source_enabled("SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL")
         timeout = int(os.getenv("REQUEST_TIMEOUT", "3"))
         return download_remote_media(image_file, timeout=timeout)
-    if image_file.startswith(("file://", "/")):
-        with open(image_file, "rb") as f:
-            return f.read()
-    if isinstance(image_file, str) and image_file.startswith("data:"):
+    if scheme == "data":
         _, encoded = image_file.split(",", 1)
         return pybase64.b64decode(encoded, validate=True)
-    if isinstance(image_file, str):
-        return pybase64.b64decode(image_file, validate=True)
-    raise NotImplementedError(f"Invalid image: {image_file}")
+    if scheme == "file":
+        _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
+        with open(unquote(urlparse(image_file).path), "rb") as f:
+            return f.read()
+    if scheme:
+        _reject_unsupported_media_scheme(image_file, scheme)
+    if os.path.isabs(image_file) or os.path.isfile(image_file):
+        _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
+        with open(image_file, "rb") as f:
+            return f.read()
+    return pybase64.b64decode(image_file, validate=True)
 
 
 def _normalize_video_input(
@@ -2008,15 +2080,21 @@ def _normalize_video_input(
     if isinstance(video_file, bytes):
         return video_file
     elif isinstance(video_file, str):
-        if video_file.startswith(("http://", "https://")):
+        scheme = _media_source_scheme(video_file)
+        if scheme in {"http", "https"}:
+            _require_media_source_enabled("SGLANG_ALLOW_REMOTE_MEDIA", "Remote URL")
             timeout = int(os.getenv("REQUEST_TIMEOUT", "10"))
             return download_remote_media(video_file, timeout=timeout)
-        elif video_file.startswith("data:"):
+        elif scheme == "data":
             _, encoded = video_file.split(",", 1)
             return pybase64.b64decode(encoded, validate=True)
-        elif video_file.startswith("file://"):
+        elif scheme == "file":
+            _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
             return unquote(urlparse(video_file).path)
-        elif os.path.isfile(unquote(urlparse(video_file).path)):
+        elif scheme:
+            _reject_unsupported_media_scheme(video_file, scheme)
+        elif os.path.isfile(video_file):
+            _require_media_source_enabled("SGLANG_ALLOW_LOCAL_MEDIA", "Local file")
             return video_file
         else:
             return pybase64.b64decode(video_file, validate=True)
