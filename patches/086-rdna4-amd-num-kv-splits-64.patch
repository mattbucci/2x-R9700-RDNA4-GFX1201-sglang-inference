diff --git a/python/sglang/srt/arg_groups/platform_hook.py b/python/sglang/srt/arg_groups/platform_hook.py
index 67007b734..6068cc500 100644
--- a/python/sglang/srt/arg_groups/platform_hook.py
+++ b/python/sglang/srt/arg_groups/platform_hook.py
@@ -65,8 +65,19 @@ def handle_mps_backends(server_args: Any):
 
 def handle_amd_specifics(server_args: Any):
     if get_platform().is_hip:
+        # gfx1201 R9700 = 64 CUs. The Triton flash-decode grid is
+        # head_groups x num_kv_splits at batch=1 decode, so the old default of 16
+        # under-fills the CUs (e.g. 2 head-groups -> 32 blocks) and decode attention
+        # stalls at ~21% of the KV-read roofline at long context. 64 lets
+        # get_num_kv_splits scale up to fill the CUs at depth: measured 2.14x 256K
+        # decode on coder-reap-25b (14.4 -> 30.7 tok/s), short context unregressed
+        # (the heuristic still picks few splits there). SGLANG_KV_SPLITS_OVERRIDE
+        # forces a value. A/B: benchmarks/profiling/coder-reap-25b-256k-kvsplit-ab.json
+        _kv = os.environ.get("SGLANG_KV_SPLITS_OVERRIDE")
         declare_resolution(
-            server_args, "_handle_amd_specifics", triton_attention_num_kv_splits=16
+            server_args,
+            "_handle_amd_specifics",
+            triton_attention_num_kv_splits=int(_kv) if _kv else 64,
         )
         # Above this the HIP runtime registers a pageable H2D source with the
         # GPU rather than staging it, and the MMU notifier on that registration
