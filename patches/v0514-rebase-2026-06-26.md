# SGLang v0.5.13.post1 → v0.5.14 patch rebase (2026-06-26)

Rebased the RDNA4 patch series onto `v0.5.14` (tag `4289f36e`) via a real `git rebase --onto`
(not hand-apply). Method: clone v0.5.14 fresh to **`/data/sgl-v0514`** (the live serving tree
`/data/sgl-rebase` was untouched — it was serving a bake-off), fetch the `r9700-v0512` patch
branch (47 commits = the v0.5.13 series) from the live repo, then
`git rebase --onto v0.5.14 v0.5.13.post1 r9700-v0514`. **Result: 45 commits replayed, 4 needed
manual resolution, 1 patch dropped (upstreamed).** Fresh env built by
`SGLANG_TAG=v0.5.14 SGLANG_DIR=/data/sgl-v0514 ENV_NAME=sglang-triton36-v0514 scripts/setup.sh`.

> ⚠ **NOT promoted to live yet.** Live remains `/data/sgl-rebase` (v0.5.13.post1, env
> `sglang-triton36-v0513`) until GPU validation passes. Promote = flip `scripts/common.sh`
> `ENV_NAME`/`SGLANG_DIR` after the resweep, per the v0513 model.

## The v0.5.14 theme: `get_attention_tp_rank()` → `get_parallel().attn_tp_rank`
v0.5.14 introduced a `ParallelContext` accessor (`sglang/srt/runtime_context.py:get_parallel()`)
and migrated TP-rank/size call sites from the free functions `get_attention_tp_rank()` /
`get_attention_tp_size()` (still defined in `layers/dp_attention.py`, just no longer the idiom)
to `get_parallel().attn_tp_rank` / `.attn_tp_size`. This is what generated 3 of the 4 conflicts.
Resolution rule: **adopt `get_parallel()` at every site touched** (already imported in the
affected files; semantically identical) while preserving our RDNA4 deltas.

## Dropped (1 — upstreamed)
- **065** (rdna4-split-kv-tree-verify) — **v0.5.14 upstreamed split-KV EAGLE-verify natively**:
  `layers/attention/triton_ops/verify_splitkv.py` (27 KB) + `TritonAttnBackend.verify_splitkv_fwd`
  + `use_verify_splitkv` gated on `envs.SGLANG_ENABLE_SPLITKV_VERIFY` (default OFF, same as our
  opt-in `SGLANG_TREE_VERIFY_SPLITKV`). Our 065 was a CANDIDATE-level opt-in perf path (flag-off,
  inert in the default serving path), and upstream's is the same idea built from scratch for ROCm.
  Both fall through to the stock extend kernel when they can't serve, so taking the native path is
  correctness-safe. Took HEAD for the `triton_backend.py` integration conflict, dropped both 065
  commits (the 355-line `tree_verify_attention.py` kernel + the integration), archived
  `065-rdna4-split-kv-tree-verify.patch.SUPERSEDED`. **Revisit only if** the resweep shows v0.5.14's
  native `verify_splitkv` is broken/slower on gfx1201 specifically (then revive 065 as a CANDIDATE).

## Conflict resolutions (4)
- **001** (upstream-sync) — 3 conflicts:
  - `qwen3_5.py` — kept our **DeltaNet replication delta** (`attn_tp_rank=0 / attn_tp_size=1` +
    `real_tp_rank/real_tp_size` to avoid FP16 rounding through recurrent state S(t)); migrated
    `real_tp_*` and the 2 auto-merged sibling sites (`Qwen3_5GatedDeltaNet`/dense + MoE decoder)
    to `get_parallel().attn_tp_*` so the file needs no `get_attention_tp_*` import.
  - `weight_utils.py` — kept our `override_tp_rank` param on `sharded_weight_loader`; combined with
    v0.5.14's accessor → `override_tp_rank if … else get_parallel().attn_tp_rank`.
  - `model_runner_kv_cache_mixin.py` — took HEAD: v0.5.14 moved `swa_head_dim`/`swa_v_head_dim` onto
    `model_config` natively (was our local-var workaround); the `v_head_dim` line is native here too.
- **005** (fp8-fallbacks) — `fp8_utils.py` add/add: v0.5.14 inserted `_FORCE_CK_W8A8` / `set_force_ck_w8a8`
  (DeepseekV4 CK bpreshuffle) at the same spot our patch inserts `_is_rdna4_device()`. **Kept both.**
- **028** (moe-runner-imports) — `moe_runner/triton.py` add/add on a `from sglang.srt.utils import …`
  line: v0.5.14 wants `is_gfx95_supported`, ours wants `cpu_has_amx_support/is_cpu/is_xpu`. All 6
  symbols are used in the body → imported the **union**.
- **065** integration — see Dropped, above (took HEAD).

## Remnant-fix carryover verified (the v0.5.13 2nd-pass fixes still valid on v0.5.14)
- **059** (fuseep drop) — `token_dispatcher/__init__.py` has no `fuseep`/`NpuFuseEP` ✓
- **061** (gemma4_mm PP-wrap) — `if self.pp_group.is_first_rank:` still wraps `vision_tower`
  construction with `else: PPMissingLayer()`, audio_config nested inside ✓
- **060** (aiter mxfp4-moe guard), **062–064** (cohere2/relu2/ministral3) — applied as commits;
  confirmed at the eager-import smoke.

## Equivalence gate (the 3-gate audit)
- **Gate 2** (pristine replay): all 45 patches apply to a fresh `v0.5.14` worktree, **45/45 applied,
  0 skipped, 0 failed**.
- **Gate 1** (byte-equality): the patched pristine tree == `/data/sgl-v0514` `r9700-v0514` HEAD,
  **git-diff empty** ✓
- **Gate 3** (no double-apply): every patch's `git apply --check` FAILS on the patched tree **except
  026**. **026 is a benign, pre-existing exception** (not a regression): v0.5.14's `gemma4_mm.py` —
  like v0.5.13's — has two near-identical `pooled, pooler_mask = vt(pv, pp)` sites (`get_image_feature`
  per-frame-patched + `get_video_feature` left batched), so 026's anchor is non-unique. The placement
  is **byte-identical to the validated live v0.5.13 tree** (which passed video 4/4), and our setup.sh
  never re-applies on an already-patched tree, so the double-apply path doesn't exist in practice.

## Series
**46 active patches** (was 46 on v0.5.13; 065 archived → `.SUPERSEDED`, +073 added in the resweep).
Numbers: 001–008, 011, 015–016, 023–028, 030–033, 036–037, 039–049, 055–064, 066, **073**.

**073-rdna4-mamba-extra-buffer-hip-fallback (found in the post-promotion resweep, 2026-06-26).**
v0.5.14 added `_validate_mamba_extra_buffer` asserting `is_cuda() or is_musa() or is_npu()` (the new
mamba-radix-cache `extra_buffer` strategy uses an FLA kernel absent on ROCm), but `_handle_mamba_radix_cache`'s
`auto` selector picked `extra_buffer` on HIP regardless → **every DeltaNet/mamba model crashed at server-args
init** (`AssertionError: extra_buffer needs CUDA/MUSA/NPU (FLA)`). Fix: gate `auto`→`extra_buffer` on
`(is_cuda or is_musa or is_npu)`; HIP falls back to `no_buffer` (the supported path). Same ROCm-allowlist
class as 031/044 → PR-candidate. Validated: qwen36-moe was CRASH → READY, caps 3/3, 32/36, 55.9 tok/s,
`mamba_radix_cache_strategy='no_buffer'`. The initial 2-model validation (coder-30b + gemma4) couldn't catch
this — neither is DeltaNet/mamba; the resweep's per-preset boot is what surfaced it.

## Validation (env `sglang-triton36-v0514`, tree `/data/sgl-v0514`)
**Eager-import boot-chain smoke (CPU, the merge-remnant catch): 21/21, 0 fail.** `QUANTIZATION_METHODS`,
`launch_server`, the quark/fp8/moe_wna16/hybrid_w4a16 quant modules, `token_dispatcher` (fuseep),
`activation` (relu2), `triton_backend`, and every patched model (qwen3_5/next/moe/vl, gemma4_mm/causal,
ministral3, llama, glm4_moe, cohere2_moe, nemotron_h) import clean. No aiter/fuseep/relu2/PP-wrap remnants.

**GPU (TP=2, both R9700):**
- **coder-30b** (`Qwen3-Coder-30B-A3B-AWQ-native`, MoE-AWQ `moe_wna16`, cuda-graph OFF): boots,
  `max_total_num_tokens=820795`; codegen **STRONG** (`is_prime` correct via `/generate`);
  **`eval_comprehensive` 35/36** — knowledge+reasoning 7/7, edge 4/5 (the 1 miss is the model's
  `-7 % 3 → -1` C-vs-Python quirk, not serving), **parallel-stress 8/8** (concurrent batching).
  Validates the core stack: `moe_wna16` loader + `awq_gemv` + Triton-MoE + sampler + triton-attention + NCCL.
- **gemma4-26B** (`gemma-4-26B-A4B-it-AWQ`, `moe_wna16` + hybrid-SWA, cuda-graph deferred via skip-warmup):
  `validate_capabilities` **4/4** — basic (`paris`), **thinking** (509 reasoning-tok, terminated),
  **vision** (read the red circle), **video** (described motion). Confirms the gemma4 multimodal patches
  (023–026, 061 PP-wrap) survived functionally, and the preserve-modalities mandate holds on v0.5.14.

## ✅ RESOLVED: v0.5.14 cuda-graph **warmup-capture hang** on RDNA4 = missing RCCL pre-warm
v0.5.14 restructured the cuda-graph system into `CudaGraphConfig(decode=PhaseConfig(backend='full'…))`
(`model_executor/cuda_graph_config.py`; backends `full|breakable|tc_piecewise|disabled`) and **deprecated
`--disable-cuda-graph`** → `--cuda-graph-backend-{decode,prefill}=disabled`. The **`backend='full'` decode
capture wedged at warmup on RDNA4**: coder-30b booted, printed "ready", then hung before serving — TP0 `R`/busy,
TP1 `__futex_wait`, GPU idle-low, port dead.

**Root cause (server_args.py):** v0.5.14's FULL backend captures the TP all-reduce **into** the graph; the
RCCL communicator's channel-init runs *during* `hipGraph` capture and deadlocks at TP=2. The flag that
prevents this — `pre_warm_nccl` (one warmup `all_reduce` before capture, `model_runner.py:1244`) — has a
docstring saying *"Default: enabled for AMD/HIP (RCCL)"* but **the code never enables it** (field default
`False`; `_handle_nccl_pre_warm()` only ever turns it *off*). So on RDNA4 it stayed off → capture deadlocked.

**Fix (`scripts/launch.sh`, 2026-06-26):** pass **`--pre-warm-nccl`** unconditionally in the CMD builder
(HIP-only-applicable; a ~0.2s no-op at TP=1 / cuda-graph-off). Validated end-to-end through the default
`launch.sh coder-30b` path: `NCCL/RCCL warmup completed in 0.22s` → `Capture target decode CUDA graph begin
backend=full bs=[1,2,4,8,12,16,24]` → ready → **serves codegen STRONG at 53.9 tok/s vs ~24 eager (~2.2×),
health 200 after** (no wedge). The cuda-graph decode speedup is restored. gemma4 (skip-warmup → eager) is
unaffected and still 4/4. **Upstream PR-candidate:** default `pre_warm_nccl=True` on HIP per its own docstring
(small ROCm one-liner, same class as 031/044) — left as launch-flag for now (lower risk than overriding a
possible explicit `--no-pre-warm-nccl`).

## Promotion status: **STAGED, ready to promote after a 256K resweep**
The cuda-graph blocker is resolved. `scripts/common.sh` still points at the v0.5.13 live stack
(`sglang-triton36-v0513` / `/data/sgl-rebase`); promote = flip `ENV_NAME`/`SGLANG_DIR` →
`sglang-triton36-v0514` / `/data/sgl-v0514` **after** a per-model 256K resweep through the default launch.sh
path (re-confirm the other cuda-graph-on presets — qwen36-moe etc. — capture clean with the pre-warm flag).
Rollback stays the untouched v0.5.13 stack.
