diff --git a/python/sglang/srt/configs/qwen3_next.py b/python/sglang/srt/configs/qwen3_next.py
index 3bf153a..3315551 100644
--- a/python/sglang/srt/configs/qwen3_next.py
+++ b/python/sglang/srt/configs/qwen3_next.py
@@ -287,8 +287,11 @@ class Qwen3NextConfig(PretrainedConfig):
             world_size = get_attention_tp_size()
             adjust_tp_num_heads_if_necessary(self, world_size, False)
 
+        # DeltaNet layers are replicated across TP ranks (not sharded)
+        # to prevent FP16 precision loss in the recurrent state.
+        # Use tp_world_size=1 so each GPU holds the full state.
         shape = Mamba2StateShape.create(
-            tp_world_size=get_attention_tp_size(),
+            tp_world_size=1,
             intermediate_size=self.linear_value_head_dim * self.linear_num_value_heads,
             n_groups=self.linear_num_key_heads,
             num_heads=self.linear_num_value_heads,
diff --git a/python/sglang/srt/entrypoints/http_server.py b/python/sglang/srt/entrypoints/http_server.py
index 1d6816c..4be0304 100644
--- a/python/sglang/srt/entrypoints/http_server.py
+++ b/python/sglang/srt/entrypoints/http_server.py
@@ -1692,6 +1692,24 @@ def _execute_server_warmup(server_args: ServerArgs):
 
     # Construct a warmup request
     is_vlm = bool(model_info.get("has_image_understanding", False))
+
+    # Skip VLM (image) warmup for models with persistent recurrent state
+    # (DeltaNet, Mamba) and for AWQ-quantized VLMs where the vision encoder
+    # may produce garbage during warmup, polluting the radix cache.
+    # Text-only warmup still triggers CUDA-graph capture and kernel JIT.
+    _TEXT_ONLY_WARMUP_ARCHS = {
+        "Qwen3_5ForConditionalGeneration",
+        "Qwen3_5MoeForConditionalGeneration",
+        "JetVLMForConditionalGeneration",
+        "Mistral3ForConditionalGeneration",
+    }
+    architectures = model_info.get("architectures") or []
+    if is_vlm and any(a in _TEXT_ONLY_WARMUP_ARCHS for a in architectures):
+        logger.info(
+            "Using text-only warmup for VLM "
+            f"({architectures[0]}) to avoid radix cache pollution."
+        )
+        is_vlm = False
     if model_info["is_generation"]:
         if is_vlm and not server_args.skip_tokenizer_init:
             request_name = "/v1/chat/completions"
diff --git a/python/sglang/srt/layers/attention/mamba/causal_conv1d_triton.py b/python/sglang/srt/layers/attention/mamba/causal_conv1d_triton.py
index c82f4d7..3577564 100644
--- a/python/sglang/srt/layers/attention/mamba/causal_conv1d_triton.py
+++ b/python/sglang/srt/layers/attention/mamba/causal_conv1d_triton.py
@@ -117,33 +117,35 @@ def _causal_conv1d_fwd_kernel(  # continuous batching
         if HAS_INITIAL_STATES:  # the new HAS_INITIAL_STATES
             load_init_state = tl.load(has_initial_states_ptr + idx_seq).to(tl.int1)
         if load_init_state:
-            # load from conv_states
+            # load from conv_states, cast to activation dtype to avoid
+            # type mismatch when conv_states are bf16 but activations are fp16
             prior_tokens = conv_states_base + (state_len - 1) * stride_conv_state_tok
             mask_w = idx_feats < dim
+            _act_dtype = x_ptr.dtype.element_ty
             if KERNEL_WIDTH == 2:
                 conv_states_ptrs = prior_tokens  # [BLOCK_N]
-                col0 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col0 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
             if KERNEL_WIDTH == 3:
                 conv_states_ptrs = prior_tokens  # [BLOCK_N]
-                col1 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col1 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
                 conv_states_ptrs = prior_tokens - 1 * stride_conv_state_tok  # [BLOCK_N]
-                col0 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col0 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
             if KERNEL_WIDTH == 4:
                 conv_states_ptrs = prior_tokens  # [BLOCK_N]
-                col2 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col2 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
                 conv_states_ptrs = prior_tokens - 1 * stride_conv_state_tok  # [BLOCK_N]
-                col1 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col1 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
                 conv_states_ptrs = prior_tokens - 2 * stride_conv_state_tok  # [BLOCK_N]
-                col0 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col0 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
             if KERNEL_WIDTH == 5:
                 conv_states_ptrs = prior_tokens  # [BLOCK_N]
-                col3 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col3 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
                 conv_states_ptrs = prior_tokens - 1 * stride_conv_state_tok  # [BLOCK_N]
-                col2 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col2 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
                 conv_states_ptrs = prior_tokens - 2 * stride_conv_state_tok  # [BLOCK_N]
-                col1 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col1 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
                 conv_states_ptrs = prior_tokens - 3 * stride_conv_state_tok  # [BLOCK_N]
-                col0 = tl.load(conv_states_ptrs, mask_w, 0.0)
+                col0 = tl.load(conv_states_ptrs, mask_w, 0.0).to(_act_dtype)
         else:
             # prior-tokens are zeros
             if KERNEL_WIDTH >= 2:  # STRATEGY1
diff --git a/python/sglang/srt/layers/attention/triton_ops/decode_attention.py b/python/sglang/srt/layers/attention/triton_ops/decode_attention.py
index 1ba5d46..77c8db8 100644
--- a/python/sglang/srt/layers/attention/triton_ops/decode_attention.py
+++ b/python/sglang/srt/layers/attention/triton_ops/decode_attention.py
@@ -28,6 +28,14 @@ import triton.language as tl
 from sglang.srt.utils import is_hip
 
 _is_hip = is_hip()
+_is_rdna4 = False
+if _is_hip:
+    try:
+        import torch
+        _gpu_arch = torch.cuda.get_device_properties(0).gcnArchName
+        _is_rdna4 = "gfx1200" in _gpu_arch or "gfx1201" in _gpu_arch
+    except Exception:
+        pass
 
 logger = logging.getLogger(__name__)
 
@@ -196,7 +204,7 @@ def _decode_att_m_fwd(
     BLOCK = 64
     # [TODO] work around SGPR limit on MI3xx
     if _is_hip:
-        BLOCK = 8
+        BLOCK = 32 if _is_rdna4 else 8
     MAX_KV_SPLITS = max_kv_splits
     Lk = k_buffer.shape[-1]
     Lv = v_buffer.shape[-1]
@@ -210,7 +218,7 @@ def _decode_att_m_fwd(
         num_warps = 4
     else:
         num_warps = 2
-        if _is_hip:
+        if _is_hip and not _is_rdna4:
             num_warps = 1
 
     BLOCK_DMODEL = triton.next_power_of_2(Lk)
diff --git a/python/sglang/srt/layers/attention/triton_ops/extend_attention.py b/python/sglang/srt/layers/attention/triton_ops/extend_attention.py
index 2fb428f..fc63d06 100644
--- a/python/sglang/srt/layers/attention/triton_ops/extend_attention.py
+++ b/python/sglang/srt/layers/attention/triton_ops/extend_attention.py
@@ -30,6 +30,13 @@ if _is_cuda:
     CUDA_CAPABILITY = torch.cuda.get_device_capability()
 
 _is_hip = is_hip()
+_is_rdna4 = False
+if _is_hip:
+    try:
+        _gpu_arch = torch.cuda.get_device_properties(0).gcnArchName
+        _is_rdna4 = "gfx1200" in _gpu_arch or "gfx1201" in _gpu_arch
+    except Exception:
+        pass
 
 
 def _get_block_sizes_for_extend_attention(Lq: int, Lv: int):
@@ -60,7 +67,16 @@ def _get_block_sizes_for_extend_attention(Lq: int, Lv: int):
     BLOCK_DV = triton.next_power_of_2(Lv)
 
     # Determine BLOCK_M, BLOCK_N, and num_warps based on hardware
-    if _is_hip:
+    if _is_rdna4:
+        # RDNA4 (gfx1200/gfx1201): wave32, reduce VGPR pressure
+        if Lq <= 128:
+            BLOCK_M, BLOCK_N = (64, 64)
+        elif Lq <= 256:
+            BLOCK_M, BLOCK_N = (32, 64)
+        else:
+            BLOCK_M, BLOCK_N = (32, 32)
+        num_warps = 4
+    elif _is_hip:
         BLOCK_M, BLOCK_N = (64, 64)
         num_warps = 4
     else:
diff --git a/python/sglang/srt/layers/communicator.py b/python/sglang/srt/layers/communicator.py
index 64f2cf6..212c434 100644
--- a/python/sglang/srt/layers/communicator.py
+++ b/python/sglang/srt/layers/communicator.py
@@ -814,7 +814,17 @@ class CommunicateWithAllReduceAndLayerNormFn:
                     hidden_states, residual
                 )
             else:
-                hidden_states = tensor_model_parallel_all_reduce(hidden_states)
+                # Promote to float32 for the all-reduce to preserve precision.
+                # DeltaNet's recurrent state amplifies FP16 rounding from TP
+                # partial sums across many layers. The .float() → all-reduce
+                # → .to(orig) path keeps the sum in float32, eliminating the
+                # ~1-ULP FP16 error per layer that compounds through the SSM.
+                _dt = hidden_states.dtype
+                hidden_states = tensor_model_parallel_all_reduce(
+                    hidden_states.float() if _dt != torch.float32 else hidden_states
+                )
+                if _dt != torch.float32:
+                    hidden_states = hidden_states.to(_dt)
                 if _is_npu and context.cache is not None:
                     _ = prepare_weight_cache(hidden_states, context.cache)
                 hidden_states, residual = layernorm(hidden_states, residual)
diff --git a/python/sglang/srt/layers/moe/fused_moe_triton/layer.py b/python/sglang/srt/layers/moe/fused_moe_triton/layer.py
index de8a07a..022224a 100644
--- a/python/sglang/srt/layers/moe/fused_moe_triton/layer.py
+++ b/python/sglang/srt/layers/moe/fused_moe_triton/layer.py
@@ -37,7 +37,10 @@ from sglang.srt.layers.moe.kt_ep_wrapper import (
 )
 from sglang.srt.layers.moe.token_dispatcher import CombineInput, DispatchOutput
 from sglang.srt.layers.moe.token_dispatcher.base import BaseDispatcher
-from sglang.srt.layers.moe.token_dispatcher.flashinfer import FlashinferDispatcher
+try:
+    from sglang.srt.layers.moe.token_dispatcher.flashinfer import FlashinferDispatcher
+except (ImportError, AssertionError):
+    FlashinferDispatcher = None
 from sglang.srt.layers.moe.token_dispatcher.standard import (
     StandardDispatcher,
     StandardDispatchOutput,
diff --git a/python/sglang/srt/layers/moe/token_dispatcher/__init__.py b/python/sglang/srt/layers/moe/token_dispatcher/__init__.py
index 2095700..8757cb5 100644
--- a/python/sglang/srt/layers/moe/token_dispatcher/__init__.py
+++ b/python/sglang/srt/layers/moe/token_dispatcher/__init__.py
@@ -16,10 +16,13 @@ from sglang.srt.layers.moe.token_dispatcher.deepep import (
     DeepEPNormalCombineInput,
     DeepEPNormalDispatchOutput,
 )
-from sglang.srt.layers.moe.token_dispatcher.flashinfer import (
-    FlashinferDispatcher,
-    FlashinferDispatchOutput,
-)
+try:
+    from sglang.srt.layers.moe.token_dispatcher.flashinfer import (
+        FlashinferDispatcher,
+        FlashinferDispatchOutput,
+    )
+except (ImportError, AssertionError):
+    pass
 from sglang.srt.layers.moe.token_dispatcher.fuseep import NpuFuseEPDispatcher
 from sglang.srt.layers.moe.token_dispatcher.mooncake import (
     MooncakeCombineInput,
diff --git a/python/sglang/srt/layers/quantization/awq.py b/python/sglang/srt/layers/quantization/awq.py
index 173bc2b..acfa3fd 100644
--- a/python/sglang/srt/layers/quantization/awq.py
+++ b/python/sglang/srt/layers/quantization/awq.py
@@ -126,7 +126,7 @@ class AWQConfig(QuantizationConfig):
         return "awq"
 
     def get_supported_act_dtypes(self) -> List[torch.dtype]:
-        return [torch.float16] if not _is_npu else [torch.float16, torch.bfloat16]
+        return [torch.float16, torch.bfloat16]
 
     @classmethod
     def get_min_capability(cls) -> int:
@@ -444,8 +444,25 @@ class AWQLinearMethod(LinearMethodBase):
         pack_factor = self.quant_config.pack_factor
         out_shape = x.shape[:-1] + (qweight.shape[-1] * pack_factor,)
         reshaped_x = x.reshape(-1, x.shape[-1])
-        out = awq_dequantize(qweight, scales, qzeros)
-        out = torch.matmul(reshaped_x, out)
+
+        import os as _os
+        if _is_hip and not _os.environ.get("AWQ_USE_DECOMPOSE"):
+            # Fused AWQ GEMM — much faster than dequantize + matmul
+            # Best server throughput config for triton 3.6 + RDNA4:
+            # BSM=32 BSN=64 BSK=64, adaptive split_k by N dimension.
+            # The kernel outputs float32 to preserve accumulator precision
+            # through the TP all-reduce; we cast back to input dtype here.
+            from sglang.srt.layers.quantization.awq_triton import awq_gemm_triton
+            N = qweight.shape[1] * pack_factor
+            split_k = 2 if N > 10000 else 8
+            out = awq_gemm_triton(
+                reshaped_x, qweight, scales, qzeros,
+                split_k_iters=split_k,
+                block_size_m=32, block_size_n=64, block_size_k=64,
+            ).to(x.dtype)
+        else:
+            out = awq_dequantize(qweight, scales, qzeros)
+            out = torch.matmul(reshaped_x, out)
 
         if bias is not None:
             out.add_(bias)
diff --git a/python/sglang/srt/layers/quantization/awq_triton.py b/python/sglang/srt/layers/quantization/awq_triton.py
index b83dd79..4d76443 100644
--- a/python/sglang/srt/layers/quantization/awq_triton.py
+++ b/python/sglang/srt/layers/quantization/awq_triton.py
@@ -133,15 +133,8 @@ def awq_gemm_kernel(
     pid_m = pid // num_pid_n
     pid_n = pid % num_pid_n
 
-    accumulator_dtype = c_ptr.type.element_ty
-
-    # NOTE: This doesn't work in TRITON_INTERPRET=1 mode.  Use below instead.
-    # accumulator = tl.arange(0, BLOCK_SIZE_N)
-    # accumulator = tl.broadcast_to(accumulator[None, :],
-    # (BLOCK_SIZE_M, BLOCK_SIZE_N))
-    # accumulator = accumulator & 0x0
-    # accumulator = accumulator.to(accumulator_dtype)
-    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=accumulator_dtype)
+    # Use FP32 accumulator for precision and RDNA4 WMMA compatibility
+    accumulator = tl.zeros((BLOCK_SIZE_M, BLOCK_SIZE_N), dtype=tl.float32)
 
     # Create reverse AWQ order as tensor: [0, 4, 1, 5, 2, 6, 3, 7]
     # that will map given indices to the correct order.
@@ -212,10 +205,10 @@ def awq_gemm_kernel(
         b = (b >> shifts) & 0xF
         zeros = (zeros >> shifts) & 0xF
         b = (b - zeros) * scales
-        b = b.to(c_ptr.type.element_ty)
+        b = b.to(a.type.element_ty)
 
         # Accumulate results.
-        accumulator = tl.dot(a, b, accumulator, out_dtype=accumulator_dtype)
+        accumulator = tl.dot(a, b, accumulator, out_dtype=tl.float32)
 
         offsets_k += BLOCK_SIZE_K * SPLIT_K
         a_ptrs += BLOCK_SIZE_K * SPLIT_K
@@ -314,7 +307,13 @@ def awq_gemm_triton(
         split_k_iters,
     )
 
-    result = torch.zeros((split_k_iters, M, N), dtype=scales.dtype, device=input.device)
+    # Use float32 result buffer to preserve the kernel's float32 accumulator
+    # precision through split_k summation.  Without this, each split's
+    # float32 dot-product is rounded to float16 before the sum, introducing
+    # ~1-ULP error per split that compounds through DeltaNet's recurrent state
+    # across 48+ layers of TP all-reduce.  The caller casts back to float16
+    # after the TP all-reduce, so downstream memory usage is unchanged.
+    result = torch.zeros((split_k_iters, M, N), dtype=torch.float32, device=input.device)
 
     # A = input, B = qweight, C = result
     # A = M x K, B = K x N, C = M x N
diff --git a/python/sglang/srt/layers/quantization/fp8_utils.py b/python/sglang/srt/layers/quantization/fp8_utils.py
index 3b9616e..7b9bf74 100644
--- a/python/sglang/srt/layers/quantization/fp8_utils.py
+++ b/python/sglang/srt/layers/quantization/fp8_utils.py
@@ -84,9 +84,12 @@ def use_rowwise_torch_scaled_mm():
     if _is_hip:
         # The condition to determine if it is on a platform that supports
         # torch._scaled_mm rowwise feature.
-        # The condition is determined once as the operations
-        # are time consuming.
-        return get_device_capability() >= (9, 4) and torch_release >= (2, 7)
+        # RDNA4 (gfx12): disable rowwise torch._scaled_mm — hipBLASLt FP8 kernels
+        # can cause GPU hangs during CUDA graph capture on RDNA4.
+        capability = get_device_capability()
+        if capability[0] >= 12:  # RDNA4
+            return False
+        return capability >= (9, 4) and torch_release >= (2, 7)
     return False
 
 
diff --git a/python/sglang/srt/layers/quantization/quark/schemes/quark_w4a4_mxfp4.py b/python/sglang/srt/layers/quantization/quark/schemes/quark_w4a4_mxfp4.py
index 984e156..f9d9e1b 100644
--- a/python/sglang/srt/layers/quantization/quark/schemes/quark_w4a4_mxfp4.py
+++ b/python/sglang/srt/layers/quantization/quark/schemes/quark_w4a4_mxfp4.py
@@ -9,10 +9,13 @@ from sglang.srt.layers.quantization.quark.schemes import QuarkLinearScheme
 from sglang.srt.utils import is_hip
 
 _is_hip = is_hip()
-if _is_hip:
-    from aiter.ops.triton.gemm_afp4wfp4 import gemm_afp4wfp4
-    from aiter.ops.triton.gemm_afp4wfp4_pre_quant_atomic import gemm_afp4wfp4_pre_quant
-    from aiter.ops.triton.quant import dynamic_mxfp4_quant
+try:
+    if _is_hip:
+        from aiter.ops.triton.gemm_afp4wfp4 import gemm_afp4wfp4
+        from aiter.ops.triton.gemm_afp4wfp4_pre_quant_atomic import gemm_afp4wfp4_pre_quant
+        from aiter.ops.triton.quant import dynamic_mxfp4_quant
+except ImportError:
+    pass
 
 
 __all__ = ["QuarkW4A4MXFP4"]
diff --git a/python/sglang/srt/layers/quantization/quark_int4fp8_moe.py b/python/sglang/srt/layers/quantization/quark_int4fp8_moe.py
index 7fbbc61..20135d8 100644
--- a/python/sglang/srt/layers/quantization/quark_int4fp8_moe.py
+++ b/python/sglang/srt/layers/quantization/quark_int4fp8_moe.py
@@ -27,11 +27,14 @@ _is_hip = is_hip()
 
 
 if _is_hip:
-    from aiter import ActivationType, QuantType
-    from aiter.fused_moe import fused_moe
-    from aiter.ops.shuffle import shuffle_weight
-
-    ON_GFX950 = "gfx950" in torch.cuda.get_device_properties("cuda").gcnArchName
+    try:
+        from aiter import ActivationType, QuantType
+        from aiter.fused_moe import fused_moe
+        from aiter.ops.shuffle import shuffle_weight
+
+        ON_GFX950 = "gfx950" in torch.cuda.get_device_properties("cuda").gcnArchName
+    except ImportError:
+        pass
 
 logger = logging.getLogger(__name__)
 
diff --git a/python/sglang/srt/layers/quantization/rocm_mxfp4_utils.py b/python/sglang/srt/layers/quantization/rocm_mxfp4_utils.py
index 4659f76..7fab491 100644
--- a/python/sglang/srt/layers/quantization/rocm_mxfp4_utils.py
+++ b/python/sglang/srt/layers/quantization/rocm_mxfp4_utils.py
@@ -1,10 +1,13 @@
-from aiter.ops.triton.batched_gemm_afp4wfp4_pre_quant import (
-    batched_gemm_afp4wfp4_pre_quant,
-)
-from aiter.ops.triton.fused_mxfp4_quant import (
-    fused_flatten_mxfp4_quant,
-    fused_rms_mxfp4_quant,
-)
+try:
+    from aiter.ops.triton.batched_gemm_afp4wfp4_pre_quant import (
+        batched_gemm_afp4wfp4_pre_quant,
+    )
+    from aiter.ops.triton.fused_mxfp4_quant import (
+        fused_flatten_mxfp4_quant,
+        fused_rms_mxfp4_quant,
+    )
+except ImportError:
+    pass
 
 __all__ = [
     "fused_rms_mxfp4_quant",
diff --git a/python/sglang/srt/layers/rocm_linear_utils.py b/python/sglang/srt/layers/rocm_linear_utils.py
index 6c8a6a3..8bfd289 100644
--- a/python/sglang/srt/layers/rocm_linear_utils.py
+++ b/python/sglang/srt/layers/rocm_linear_utils.py
@@ -1,8 +1,11 @@
 import torch
-from aiter.ops.triton.fused_kv_cache import fused_qk_rope_cat_and_cache_mla
-from aiter.ops.triton.fused_qk_concat import fused_qk_rope_cat
-from aiter.ops.triton.gemm_a16w16 import gemm_a16w16
-from aiter.ops.triton.gemm_a16w16_atomic import gemm_a16w16_atomic
+try:
+    from aiter.ops.triton.fused_kv_cache import fused_qk_rope_cat_and_cache_mla
+    from aiter.ops.triton.fused_qk_concat import fused_qk_rope_cat
+    from aiter.ops.triton.gemm_a16w16 import gemm_a16w16
+    from aiter.ops.triton.gemm_a16w16_atomic import gemm_a16w16_atomic
+except ImportError:
+    pass
 
 from sglang.srt.utils import BumpAllocator
 
diff --git a/python/sglang/srt/models/llava.py b/python/sglang/srt/models/llava.py
index c76312c..77c4273 100644
--- a/python/sglang/srt/models/llava.py
+++ b/python/sglang/srt/models/llava.py
@@ -657,7 +657,10 @@ class LlavaForConditionalGeneration(LlavaBaseForCausalLM):
     ) -> Dict[str, str]:
         mapping = {}
         for config_cls in auto_model_type._model_mapping.keys():
-            archs = auto_model_type._model_mapping.get(config_cls, None)
+            try:
+                archs = auto_model_type._model_mapping.get(config_cls, None)
+            except (ValueError, AttributeError):
+                continue
             if archs is not None:
                 if isinstance(archs, tuple):
                     mapping[config_cls.__name__] = tuple(
@@ -826,6 +829,13 @@ class LlavaForConditionalGeneration(LlavaBaseForCausalLM):
 
         # Load weights directly without remapping
         for name, loaded_weight in weights:
+            # Strip leading "model." prefix if present (AWQ converted models)
+            if name.startswith("model."):
+                name = name[len("model."):]
+            # Route lm_head/embed_tokens/layers/norm to language_model
+            if name.startswith(("lm_head", "layers.", "embed_tokens", "norm.")):
+                self.language_model.load_weights([(name, loaded_weight)])
+                continue
             for part in ("language_model", "vision_tower"):
                 if name.startswith(part):
                     name = name[len(part + ".") :]
diff --git a/python/sglang/srt/models/qwen3_5.py b/python/sglang/srt/models/qwen3_5.py
index f012254..ff5b119 100644
--- a/python/sglang/srt/models/qwen3_5.py
+++ b/python/sglang/srt/models/qwen3_5.py
@@ -40,6 +40,7 @@ from sglang.srt.eplb.expert_location import ModelConfigForExpertLocation
 # Layers - Attention
 from sglang.srt.layers.attention.fla.layernorm_gated import RMSNorm as RMSNormGated
 from sglang.srt.layers.attention.mamba.mamba import mamba_v2_sharded_weight_loader
+from sglang.srt.model_loader.weight_utils import default_weight_loader
 from sglang.srt.layers.communicator import LayerCommunicator, LayerScatterModes
 from sglang.srt.layers.dp_attention import (
     get_attention_tp_rank,
@@ -97,8 +98,16 @@ class Qwen3_5GatedDeltaNet(nn.Module):
     ) -> None:
         super().__init__()
         self.config = config
-        self.attn_tp_rank = get_attention_tp_rank()
-        self.attn_tp_size = get_attention_tp_size()
+        self.real_tp_rank = get_attention_tp_rank()
+        self.real_tp_size = get_attention_tp_size()
+        # DeltaNet layers are REPLICATED across TP ranks to avoid
+        # FP16 precision loss from TP matmul splits.  The recurrent
+        # state amplifies rounding differences across 48 layers,
+        # making TP-split DeltaNet produce garbled output for any
+        # non-trivial sequence length.  Replication costs ~2.8 GB
+        # extra VRAM per GPU but yields bit-identical results to TP=1.
+        self.attn_tp_rank = 0
+        self.attn_tp_size = 1
         self.hidden_size = config.hidden_size
         self.num_v_heads = config.linear_num_value_heads
         self.num_k_heads = config.linear_num_key_heads
@@ -113,28 +122,27 @@ class Qwen3_5GatedDeltaNet(nn.Module):
         self.activation = config.hidden_act
         self.layer_norm_epsilon = config.rms_norm_eps
 
-        # Conv1d layer
+        # Conv1d layer — replicated (tp_size=1)
         self.conv_dim = self.key_dim * 2 + self.value_dim
         self.conv1d = ColumnParallelLinear(
             input_size=self.conv_kernel_size,
             output_size=self.conv_dim,
             bias=False,
             quant_config=None,
-            tp_rank=self.attn_tp_rank,
-            tp_size=self.attn_tp_size,
+            tp_rank=0,
+            tp_size=1,
             prefix=add_prefix("conv1d", prefix),
         )
         self.conv1d.weight.data = self.conv1d.weight.data.unsqueeze(1)
 
-        # Split projection layers (following vLLM's implementation)
-        # Instead of fused in_proj_qkvz and in_proj_ba, use separate layers
+        # Split projection layers — all replicated (tp_size=1)
         self.in_proj_qkv = MergedColumnParallelLinear(
             input_size=self.hidden_size,
             output_sizes=[self.key_dim, self.key_dim, self.value_dim],
             bias=False,
             quant_config=quant_config,
-            tp_rank=self.attn_tp_rank,
-            tp_size=self.attn_tp_size,
+            tp_rank=0,
+            tp_size=1,
             prefix=add_prefix("in_proj_qkv", prefix),
         )
         self.in_proj_z = ColumnParallelLinear(
@@ -142,30 +150,30 @@ class Qwen3_5GatedDeltaNet(nn.Module):
             output_size=self.value_dim,
             bias=False,
             quant_config=quant_config,
-            tp_rank=self.attn_tp_rank,
-            tp_size=self.attn_tp_size,
+            tp_rank=0,
+            tp_size=1,
             prefix=add_prefix("in_proj_z", prefix),
         )
         self.in_proj_b = ColumnParallelLinear(
             input_size=self.hidden_size,
             output_size=self.num_v_heads,
             bias=False,
-            quant_config=quant_config,
-            tp_rank=self.attn_tp_rank,
-            tp_size=self.attn_tp_size,
+            quant_config=None,
+            tp_rank=0,
+            tp_size=1,
             prefix=add_prefix("in_proj_b", prefix),
         )
         self.in_proj_a = ColumnParallelLinear(
             input_size=self.hidden_size,
             output_size=self.num_v_heads,
             bias=False,
-            quant_config=quant_config,
-            tp_rank=self.attn_tp_rank,
-            tp_size=self.attn_tp_size,
+            quant_config=None,
+            tp_rank=0,
+            tp_size=1,
             prefix=add_prefix("in_proj_a", prefix),
         )
 
-        # Conv1d weight loader setup
+        # Conv1d weight loader — replicated (tp_size=1)
         query_key_settings = (self.key_dim, 0, False)
         value_settings = (self.value_dim, 0, False)
 
@@ -179,32 +187,33 @@ class Qwen3_5GatedDeltaNet(nn.Module):
                         query_key_settings,
                         value_settings,
                     ],
-                    self.attn_tp_size,
-                    self.attn_tp_rank,
+                    1,  # tp_size=1 (replicated)
+                    0,  # tp_rank=0
                 )
             },
         )
 
-        # State parameters
+        # State parameters — full (not TP-sharded)
         self.dt_bias = nn.Parameter(
-            torch.ones(self.num_v_heads // self.attn_tp_size),
+            torch.ones(self.num_v_heads),
         )
         self.A_log = nn.Parameter(
-            torch.empty(self.num_v_heads // self.attn_tp_size),
+            torch.empty(self.num_v_heads),
         )
 
-        set_weight_attrs(self.A_log, {"weight_loader": sharded_weight_loader(0)})
-        set_weight_attrs(self.dt_bias, {"weight_loader": sharded_weight_loader(0)})
+        # Use default weight loader (full, not sharded)
+        set_weight_attrs(self.A_log, {"weight_loader": default_weight_loader})
+        set_weight_attrs(self.dt_bias, {"weight_loader": default_weight_loader})
 
         conv_weights = self.conv1d.weight.view(
             self.conv1d.weight.size(0), self.conv1d.weight.size(2)
         )
-        # RadixLinearAttention layer
+        # RadixLinearAttention layer — full head count
         self.attn = RadixLinearAttention(
             layer_id=layer_id,
-            num_q_heads=self.num_k_heads // self.attn_tp_size,
-            num_k_heads=self.num_k_heads // self.attn_tp_size,
-            num_v_heads=self.num_v_heads // self.attn_tp_size,
+            num_q_heads=self.num_k_heads,
+            num_k_heads=self.num_k_heads,
+            num_v_heads=self.num_v_heads,
             head_q_dim=self.head_k_dim,
             head_k_dim=self.head_k_dim,
             head_v_dim=self.head_v_dim,
@@ -225,16 +234,17 @@ class Qwen3_5GatedDeltaNet(nn.Module):
             dtype=config.torch_dtype,
         )
 
-        # Output projection
+        # Output projection — replicated (tp_size=1)
+        # input_is_parallel=False because we have the full value_dim
         self.out_proj = RowParallelLinear(
             self.value_dim,
             self.hidden_size,
             bias=False,
-            input_is_parallel=True,
+            input_is_parallel=False,
             reduce_results=False,
             quant_config=quant_config,
-            tp_rank=self.attn_tp_rank,
-            tp_size=self.attn_tp_size,
+            tp_rank=0,
+            tp_size=1,
             prefix=add_prefix("out_proj", prefix),
         )
 
@@ -301,6 +311,11 @@ class Qwen3_5GatedDeltaNet(nn.Module):
         core_attn_out = core_attn_out.reshape(z_shape_og)
         core_attn_out = rearrange(core_attn_out, "... h d -> ... (h d)")
         output, _ = self.out_proj(core_attn_out)
+        # Divide by real TP size so the communicator's all-reduce
+        # (which sums identical copies from all ranks) produces
+        # the correct undivided result.
+        if self.real_tp_size > 1:
+            output = output / self.real_tp_size
         return output
 
 
@@ -318,6 +333,7 @@ class Qwen3_5LinearDecoderLayer(nn.Module):
         super().__init__()
         self.config = config
         self.layer_id = layer_id
+        self.real_tp_size = get_attention_tp_size()
 
         linear_attn_quant_config = (
             None
@@ -342,12 +358,17 @@ class Qwen3_5LinearDecoderLayer(nn.Module):
             is_previous_layer_sparse = True
             is_next_layer_sparse = True
         elif config.model_type == "qwen3_5_text":
+            # Replicate MLP across TP ranks (tp_size=1) to prevent
+            # FP16 rounding from TP matmul splits feeding error into
+            # DeltaNet's recurrent state.
             self.mlp = Qwen2MoeMLP(
                 hidden_size=config.hidden_size,
                 intermediate_size=config.intermediate_size,
                 hidden_act=config.hidden_act,
                 quant_config=quant_config,
                 prefix=add_prefix("mlp", prefix.replace(".linear_attn", "")),
+                tp_rank=0,
+                tp_size=1,
             )
             is_layer_sparse = False
             is_previous_layer_sparse = False
@@ -401,6 +422,11 @@ class Qwen3_5LinearDecoderLayer(nn.Module):
             forward_batch
         )
         hidden_states = self.mlp(hidden_states, forward_batch, use_reduce_scatter)
+        # Divide by real TP size so the communicator's all-reduce
+        # (which sums identical copies from all ranks) produces
+        # the correct undivided result.
+        if self.real_tp_size > 1:
+            hidden_states = hidden_states / self.real_tp_size
 
         hidden_states, residual = self.layer_communicator.postprocess_layer(
             hidden_states, residual, forward_batch
@@ -425,6 +451,7 @@ class Qwen3_5AttentionDecoderLayer(nn.Module):
         self.hidden_size = config.hidden_size
         self.attn_tp_rank = get_attention_tp_rank()
         self.attn_tp_size = get_attention_tp_size()
+        self.real_tp_size = self.attn_tp_size
         self.total_num_heads = config.num_attention_heads
         assert self.total_num_heads % self.attn_tp_size == 0
         self.num_heads = self.total_num_heads // self.attn_tp_size
@@ -502,7 +529,8 @@ class Qwen3_5AttentionDecoderLayer(nn.Module):
             prefix=f"{prefix}.attn",
         )
 
-        # Dense MLP for non-MoE variant
+        # Dense MLP for non-MoE variant — replicated (tp_size=1) to prevent
+        # FP16 rounding from TP matmul splits feeding error into DeltaNet.
         if config.model_type == "qwen3_5_text":
             self.mlp = Qwen2MoeMLP(
                 hidden_size=config.hidden_size,
@@ -510,6 +538,8 @@ class Qwen3_5AttentionDecoderLayer(nn.Module):
                 hidden_act=config.hidden_act,
                 quant_config=quant_config,
                 prefix=add_prefix("mlp", prefix.replace(".self_attn", "")),
+                tp_rank=0,
+                tp_size=1,
             )
             is_layer_sparse = False
             is_previous_layer_sparse = False
@@ -634,6 +664,11 @@ class Qwen3_5AttentionDecoderLayer(nn.Module):
             forward_batch
         )
         hidden_states = self.mlp(hidden_states, forward_batch, use_reduce_scatter)
+        # Divide by real TP size so the communicator's all-reduce
+        # (which sums identical copies from all ranks) produces
+        # the correct undivided result.
+        if self.real_tp_size > 1:
+            hidden_states = hidden_states / self.real_tp_size
 
         hidden_states, residual = self.layer_communicator.postprocess_layer(
             hidden_states, residual, forward_batch
diff --git a/sgl-kernel/setup_rocm.py b/sgl-kernel/setup_rocm.py
index 66713bf..1e1066d 100644
--- a/sgl-kernel/setup_rocm.py
+++ b/sgl-kernel/setup_rocm.py
@@ -72,21 +72,27 @@ if torch.cuda.is_available():
 else:
     print(f"Warning: torch.cuda not available. Using default target: {amdgpu_target}")
 
-if amdgpu_target not in ["gfx942", "gfx950"]:
+is_rdna = amdgpu_target.startswith("gfx1")
+if amdgpu_target not in ["gfx942", "gfx950"] and not is_rdna:
     print(
-        f"Warning: Unsupported GPU architecture detected '{amdgpu_target}'. Expected 'gfx942' or 'gfx950'."
+        f"Warning: Unsupported GPU architecture detected '{amdgpu_target}'. Expected 'gfx942', 'gfx950', or gfx1xxx (RDNA)."
     )
     sys.exit(1)
 
-fp8_macro = (
-    "-DHIP_FP8_TYPE_FNUZ" if amdgpu_target == "gfx942" else "-DHIP_FP8_TYPE_E4M3"
-)
+# FP8 type: gfx942 (MI300) uses FNUZ, RDNA4 and gfx950 use standard E4M3
+if amdgpu_target == "gfx942":
+    fp8_macro = "-DHIP_FP8_TYPE_FNUZ"
+else:
+    fp8_macro = "-DHIP_FP8_TYPE_E4M3"
 
 # Dynamic shared-memory budget for the TopK kernels.
 # - gfx942 (MI300/MI325): LDS is typically 64KB per workgroup -> keep dynamic smem <= ~48KB
-#   (leaves room for static shared allocations in the kernel).
-# - gfx95x (MI350): LDS is larger (e.g. 160KB per CU) -> allow the original 128KB dynamic smem.
-topk_dynamic_smem_bytes = 48 * 1024 if amdgpu_target == "gfx942" else 32 * 1024 * 4
+# - gfx95x (MI350): LDS is larger -> allow 128KB dynamic smem
+# - RDNA4 (gfx1201): LDS is 64KB per workgroup -> use 48KB like gfx942
+if is_rdna or amdgpu_target == "gfx942":
+    topk_dynamic_smem_bytes = 48 * 1024
+else:
+    topk_dynamic_smem_bytes = 32 * 1024 * 4
 
 hipcc_flags = [
     "-DNDEBUG",
