# SGLang v0.5.14 → v0.5.15 patch rebase (2026-07-11)

Rebased the RDNA4 patch series onto `v0.5.15` (tag `d80c135a`, commit `f63458b5`) via a real
`git rebase --onto` (not hand-apply). Method: `git clone --no-hardlinks` the live `/data/sgl-v0514`
tree to a fresh **`/data/sgl-v0515`** (the live serving tree was untouched — it was serving
North-Mini-Code), which brought the `r9700-v0514` patch branch + both tags along; then
`git checkout -b r9700-v0515 origin/r9700-v0514` and
`git rebase --onto v0.5.15 v0.5.14 r9700-v0515`. **Result: 48 commits attempted → 47 replayed,
1 dropped (upstreamed), 4 needed manual resolution.** Fresh env built by
`SGLANG_TAG=v0.5.15 SGLANG_DIR=/data/sgl-v0515 ENV_NAME=sglang-triton36-v0515 scripts/setup.sh`.

Upstream delta v0.5.14→v0.5.15 is **large: 547 commits, 1789 files, +145K/-39K** — yet only 4
patches conflicted, because our deltas live on RDNA4/HIP-specific branches upstream rarely touches.

> ⚠ **NOT promoted to live yet at time of writing.** Live remains `/data/sgl-v0514` (v0.5.14, env
> `sglang-triton36-v0514`) until GPU validation passes. Promote = flip `scripts/common.sh`
> `ENV_NAME`/`SGLANG_DIR` after the resweep, per the v0514 model. **Rollback** = the retained,
> untouched v0.5.14 stack: tree `/data/sgl-v0514` + env `sglang-triton36-v0514`
> (`ENV_NAME=sglang-triton36-v0514 SGLANG_DIR=/data/sgl-v0514 scripts/launch.sh …`).

## The v0.5.15 themes (what generated the 4 conflicts)
1. **MoE topk dispatch — new `_is_cuda` JIT fused-gate router.** v0.5.15 added
   `elif _is_cuda and envs.SGLANG_OPT_USE_JIT_KERNEL_FUSED_TOPK.get():` (unified Triton router,
   subsumes the AOT `topk_softmax` CUDA kernel) at the exact spot our 004 inserts the `_is_hip`
   RDNA4 torch fallback. Add/add.
2. **FP8 bpreshuffle scale materialization.** v0.5.15 added
   `materialize_bpreshuffle_fp8_scale` / `_tuple` (gfx95 bpreshuffle GEMM scale layout) where our
   005 inserts `_is_rdna4_device()`. Add/add.
3. **`UnlimitedOCRForCausalLM` hybrid-SWA.** v0.5.15 added this arch to the `hybrid_swa_archs` set
   AND a matching `elif` in `get_hybrid_layer_ids()` — the exact two spots our 062 adds
   `Cohere2MoeForCausalLM`. Add/add ×2.
4. **mamba-radix-cache resolution moved to a post-process pass.** v0.5.15 pulled the
   `auto`→`extra_buffer`/`no_buffer` decision out of `ServerArgs._handle_mamba_radix_cache` (now a
   thin `run_post_process_pass(_mamba_radix_cache_resolution)` + `view` reader) into
   `arg_groups/overrides.py:_mamba_radix_cache_resolution(view)`. Our 073's target block no longer
   exists in server_args.py.

## Conflict resolutions (4)
- **004** (rdna4-moe-fixes) — `topk.py` add/add: **kept both** branches (upstream `_is_cuda` JIT
  router first, our `_is_hip` RDNA4 torch fallback second — mutually exclusive; `else: topk_softmax`
  tail unchanged). The other 5 hunks (`topk_softmax`/`topk_sigmoid` `None`-guards,
  3× `disable=_is_npu or _is_hip`) auto-applied. Note: 004 also creates
  `configs/triton_3_7_0 -> triton_3_6_0` (our config-lookup shim so a triton-3.7 env reuses our
  R9700 3.6.0 tunings — confirmed NOT an upstream file via `git cat-file`).
- **005** (rdna4-fp8-fallbacks) — `fp8_utils.py` add/add: **kept both** (upstream's
  `materialize_bpreshuffle_fp8_scale*` + our `_is_rdna4_device()`). Other hunks (rowwise
  `cap < (10,0)` RDNA4 guard, `_dispatch_auto_backend` RDNA4 torch branch,
  `torch_w8a8_block_fp8_linear`) auto-applied.
- **062** (cohere2moe hybrid-SWA) — `model_config.py` 2× add/add: **kept both** entries/branches
  (upstream `UnlimitedOCRForCausalLM` + our `Cohere2MoeForCausalLM`). Version-neutralized the
  comment ("v0.5.13" → "Upstream"): v0.5.15 STILL does not classify Cohere2Moe as hybrid-SWA, so
  062 is still load-bearing for North-Mini (window_kv_indptr=None → cuda-graph AttributeError
  otherwise).
- **073** (mamba-extra-buffer HIP fallback) — **RELOCATED** `server_args.py` → `arg_groups/overrides.py`.
  Took HEAD's refactored structure in server_args.py (our old inline block is gone, no net delta),
  and moved the RDNA4 guard `and (is_cuda() or is_musa() or is_npu())` into the new
  `_mamba_radix_cache_resolution` `auto` branch (is_cuda/is_musa/is_npu already imported there). Same
  behavior: HIP `auto` → `no_buffer` (the FLA-free supported path); an *explicit*
  `--mamba-radix-cache-strategy extra_buffer` on HIP still hits `_validate_mamba_extra_buffer`'s
  assert (loud, correct). The `.patch` filename is unchanged; its target file changed.

## Dropped (1 — upstreamed in v0.5.15)
- **064** (ministral3 keyword super().__init__) — v0.5.15 **upstreamed the exact fix**: it now
  keyword-binds `Ministral3Attention` / `Ministral3DecoderLayer` `super().__init__()` AND threads
  the new `start_layer` param (a *superset* of our v0.5.13 patch, which omitted start_layer; the
  post-conflict tail even *uses* `start_layer`). Took HEAD → commit empty → dropped. `064-*.patch`
  archived `.SUPERSEDED`. **Lesson applied:** verified this is a true upstreaming (upstream's version
  is behaviorally complete + correct), not a coincidental empty — and it carries no RDNA4-specific
  gate to worry about (unlike 065 on the v0514 rebase).

## Equivalence gate (the 3-gate audit) — ALL PASS
- **Gate 2** (pristine replay): all 47 regenerated `.patch` files apply **strictly** (`git apply`,
  no fuzz) to a fresh `v0.5.15` worktree, in filename order, **47/47 applied, 0 failed**.
- **Gate 1** (byte-equality): the patched pristine tree == `/data/sgl-v0515` `r9700-v0515` HEAD,
  **`git diff` EMPTY** ✓ — the regenerated patch series is a faithful source of truth.
- **Gate 3** (no double-apply): every patch's `git apply --check` FAILS on the patched tree **except
  026** — the **same benign, pre-existing exception** as v0.5.14: `gemma4_mm.py` has two
  near-identical `pooler_mask = vt(pv, pp)` sites (image per-frame-patched + video batched), so 026's
  anchor is non-unique. setup.sh never re-applies on an already-patched tree, so the double-apply
  path doesn't exist in practice.

Note the rebase commit order ≠ filename order for a few patches (059/060/061 committed before
055-058; 065/066/072/073 at the tail) — benign because those patches touch **disjoint files**, so
filename-order application still reproduces the tree byte-for-byte (Gate 1 confirms).

## Series
**47 active patches** (064 archived → `.SUPERSEDED`). Numbers: 001–008, 011, 015–016, 023–028,
030–033, 036–037, 039–049, 055–063, 065–066, 072–073. (050 + 067–071 remain `.CANDIDATE` as before.)

## Validation (env `sglang-triton36-v0515`, tree `/data/sgl-v0515`)

**Eager-import boot-chain smoke (CPU, `scripts/eval/import_smoke.py`): 29/29 PASS, 0 FAIL.**
Every patched module imports clean — quant infra (`QUANTIZATION_METHODS`, fp8_utils/005, moe_wna16,
awq, modelopt/044, quark-mxfp4/060), MoE dispatch (topk/004, token_dispatcher/059, hybrid_w4a16/032),
activation (relu2/063), triton_backend (011/065), server_args + `arg_groups.overrides` (073),
model_config (062), launch_server, and every patched model (qwen3_5/next/vl, gemma4_mm/causal/unified,
ministral3, llama, glm4_moe, cohere2_moe, nemotron_h, mistral). No merge remnants.

**GPU (TP=2, both R9700) — 4 presets covering all 4 conflict resolutions + the preserve mandate:**
- **coder-30b** (`Qwen3-Coder-30B-A3B-AWQ-native`, moe_wna16, **cuda-graph ON**): NCCL/RCCL warmup 0.233s
  → decode CUDA-graph capture (backend=full, bs=[1..24]) end 12.1s, **no wedge** (the v0.5.14 pre-warm-nccl
  fix holds on v0.5.15); `pre_warm_nccl=True`; KV pool `max_total_num_tokens=817024`. codegen `is_prime`
  correct; **`eval_comprehensive` 35/36** (knowledge+reasoning 7/7, edge 4/5, parallel-stress 8/8) — the
  1 miss is the identical `-7%3→-1` C-vs-Python model quirk (not serving), **exact parity with v0.5.14**.
  Validates the core stack: moe_wna16 + awq_gemv + Triton-MoE + sampler + triton-attn + NCCL + cuda-graph
  + **patch 004** (topk `_is_hip`).
- **qwen36-moe** (`Qwen3.6-35B-A3B-AWQ`, DeltaNet-MoE moe_wna16, thinking+vision, cuda-graph ON):
  **`mamba_radix_cache_strategy='no_buffer'`** — **patch 073's relocation to `overrides.py` WORKS**
  (the DeltaNet-MoE boots via `MambaRadixCache hybrid_ssm=True` instead of the `extra_buffer` FLA-assert
  crash). Mamba cache allocated; cuda-graph capture 11.1s clean on recurrent state; KV pool 3.38M @262K.
  **`validate_capabilities` 3/3**: basic, **thinking** (1322 reasoning tok, terminated), **vision** (red
  circle). Validates **patch 073** + DeltaNet + thinking + vision preservation.
- **gemma4** (`gemma-4-26B-A4B-it-AWQ`, moe_wna16 + gelu-MoE + hybrid-SWA, cuda-graph ON):
  `SWARadixCache hybrid_swa=True`; cuda-graph capture 19.0s clean. **`validate_capabilities` 4/4** — basic,
  **thinking** (550 tok), **vision** (red circle + black outline), **video** (red circle moving across white
  bg). Validates the gemma4 multimodal stack (023-026/061) + gelu-MoE (033) + hybrid-SWA + **the full
  preserve mandate incl. video**.
- **North-Mini** (`North-Mini-Code-1.0-fp8`, Cohere2MoeForCausalLM, **FP8 compressed-tensors**,
  cuda-graph max_bs=1): **`SWARadixCache hybrid_swa=True`** — **patch 062 WORKS** (Cohere2Moe classified
  hybrid-SWA → `window_kv_indptr` populated → cuda-graph capture 3.4s clean, no `AttributeError`).
  reasoning_parser=cohere_command4; thinking trace separated (191 tok), content correct (80 km/h), **no
  delimiter leak**, finish=stop. Validates **patch 062** + **patch 005** (FP8). Left running on port 30000
  = the restored production server on the promoted stack.

**Coverage:** 004 (every MoE) · 005 (North-Mini FP8) · 062 (North-Mini SWARadixCache) · 073 (qwen36-moe
no_buffer) — all four conflict resolutions confirmed end-to-end; thinking+image+video preserved.

## Promotion
Promoted to live **2026-07-11**: `scripts/common.sh` `ENV_NAME`/`SGLANG_DIR` flipped to
`sglang-triton36-v0515` / `/data/sgl-v0515`. **Rollback** = the retained, untouched v0.5.14 stack
(`/data/sgl-v0514`, env `sglang-triton36-v0514`). A deep-context 256K per-model resweep through the
default launch path remains a follow-up (parity confirmed at the boot+caps level here).
