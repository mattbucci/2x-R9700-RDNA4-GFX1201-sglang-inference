diff --git a/python/sglang/kernels/ops/attention/decode_attention.py b/python/sglang/kernels/ops/attention/decode_attention.py
index 1099a121a..f64773947 100644
--- a/python/sglang/kernels/ops/attention/decode_attention.py
+++ b/python/sglang/kernels/ops/attention/decode_attention.py
@@ -734,8 +734,11 @@ def _fwd_grouped_kernel_stage1(
             if IS_GFX1250:
                 acc += tl.dot(p, v.to(tl.float32), out_dtype=tl.float32)
             else:
-                # RDNA4 (gfx1201): same fp32 P.V as the gfx1250 branch above.
-                acc += tl.dot(p.to(tl.float32), v.to(tl.float32))
+                # gfx1201: bf16 PV (fp32 accumulate), not fp32. fp32 PV inflated VGPR pressure
+                # -> low occupancy -> poor KV-load latency hiding, capping decode-attn at ~51%
+                # of the KV-read roofline. bf16 measured +21% at 256K decode on coder-reap-25b
+                # (33.2->40.2 tok/s), short ctx +~1%, deep recall unchanged. Standard flash-attn.
+                acc += tl.dot(p.to(tl.bfloat16), v.to(tl.bfloat16))
 
             e_sum = e_sum * re_scale + tl.sum(p, 1)
             e_max = n_e_max
