# SGLang v0.5.12 → v0.5.13.post1 patch rebase (2026-06-16)

Rebased the 37-patch RDNA4 series onto `v0.5.13.post1` (tag `1ec9d4b7`) via a real git 3-way rebase
(not hand-apply): built a `v0.5.12 + 37-patch-commits` branch (gate-1: **all 37 apply clean to
pristine v0.5.12**), then `git rebase --onto v0.5.13.post1 v0.5.12`. Result: **34 applied, 3 dropped.**
Rebased tree: `/data/sgl-rebase` (branch `r9700-v0512` → detached HEAD `dd9cb1e`). Fresh env built by
`SGLANG_TAG=v0.5.13.post1 SGLANG_DIR=/data/sgl-rebase ENV_NAME=sglang-triton36-v0513 scripts/setup.sh`
(working `sglang-triton36` + `/data/vG` left untouched until validated).

## Dropped (3)
- **012** (rdna4-sliding-window-decode-fix) — v0.5.13's `torch_native_backend.py` has its OWN native
  SWA-decode (via `sliding_window_size`/`encoder_lens`), superseding our `translate_loc_from_full_to_swa`
  approach. torch_native is the *fallback* path (production gemma = triton). Scorecard-confirmed drop.
- **034** (sampler-inf-detection) — v0.5.13 **removed** the `use_nan_detection` flag our block guards on
  (scorecard's known "not cleanly rebasable" case). Dropping = stock v0.5.13 sampler (stock v0.5.12 also
  had no native nan/inf guard — 034 *added* it). ⚠ **Re-implement as the planned standalone opt-in
  logits-sanity PR** (co-designed with 3090) if HSAIL-on-±Inf recurs in validation.
- **035** (gemma4-mm-per-expert-awq-mapping) — v0.5.13's `gemma4_mm.py` has a **native per-expert mapping**
  (`make_expert_params_mapping`, handles per-expert FP8/NVFP4/BF16-fused + AWQ-suffix via `name.replace`).
  Subsumed. ⚠ see the gemma4-AWQ validation flag below.

## Notable conflict resolutions (12 patches needed merge)
- **001** (upstream-sync) — mostly took HEAD (v0.5.13 has the backports natively); kept our RDNA4 deltas:
  the `_should_replicate_deltanet_for_tp` helper (qwen3_5, add/add → both) and the gemma4 `if False`
  fused-kernel disable (gfx1201 crash). triton SWA bits are native in v0.5.13 → HEAD.
- **004** (moe-fixes) — topk: both (v0.5.13 FlashInfer-LoRA-pack elif + our `_is_hip` torch-topk elif);
  token_dispatcher: theirs (our try/except FlashInfer guard); **moe_align: 3-way merge** — v0.5.13 added a
  JIT-align branch but calls `sgl_moe_align_block_size` directly; merged to `if use_jit … elif sgl…is not
  None … else <our torch fallback>` (the RDNA4 sgl-op-None fallback preserved).
- **005** (fp8-fallbacks) — quark_w4a4_mxfp4: theirs (our try/except AITER-import guard; v0.5.13 expanded
  it to an unguarded AITER path that crashes the module import on gfx1201). Quark-mxfp4 unused on RDNA4.
- **024** (gemma4 towers no-quant) — theirs (vision-critical `quant_config=None` on the multimodal towers
  preserved; 4 instances). v0.5.13's new PP-rank wrapping dropped — **immaterial at PP=1** (our config).
- **027** (softcap-fp32) — the kernel **moved** to `triton_ops/softcap.py` in v0.5.13; logits_processor
  conflict → HEAD, and the FP32 fix **relocated** into v0.5.13's `softcap.py` `softcap_inplace_logits`
  kernel (was doing lossy BF16 tanh).
- **031** (moe_wna16 ROCm allowlist) — both (v0.5.13 `quark_mxfp4` entry + our `moe_wna16` entry).
- **045** (CT-FP8 deltanet TP-split) — both (our `_is_compressed_tensors_fp8` helper, add/add at the same
  qwen3_5 spot as 001). DeltaNet-TP helpers all present post-merge.
- 002 (theirs: torch.compile disable=True), plus 003/006-011/013-023/025/026/028-033/036-044/046-049
  applied clean or via 3-way auto-merge.

## ⚠ Flagged for validation (verify before promoting over /data/vG)
1. **gemma4-AWQ expert loading** — took v0.5.13's native per-expert mapping (001 + dropped 024/035 overlap).
   It folds AWQ generically via `name.replace(weight_name, param_name)` (suffix `.qweight/.qzeros/.scales`
   preserved), so it *should* cover AWQ — but MUST verify: `check_awq_scales.py` + a gemma4-AWQ vision
   capability probe. If it doesn't load AWQ expert weights, graft our AWQ `is_expert_weight` handling.
2. **034 dropped** — no sampler nan/inf safety net (= stock v0.5.13). Watch for HSAIL-0x1016-on-±Inf.
3. **024 PP-wrap dropped** — fine at PP=1; re-add the `if self.pp_group.is_first_rank:` wrap if PP is used.

## To finalize (after the fresh env validates)
- Regenerate `patches/[0-9]*.patch` from the rebased commits (`git format-patch v0.5.13.post1..HEAD` in
  /data/sgl-rebase) — replacing the v0.5.12 series; keep the dropped 012/034/035 numbers retired in the map.
- Bump `setup.sh` default `SGLANG_TAG` to v0.5.13.post1; update README/PATCHES.md infra version + the
  upstream-lifecycle scorecard (012/035 now retired-to-upstream; 034 → "re-implement"; 028/030/042/043 —
  re-check the 3-gate audit for any that became no-ops on v0.5.13).
- Re-apply the 9 `.CANDIDATE`s (NGRAM 058 / cohere2 051-054 / devstral 056 / nemotron 057 / eagle3 055 /
  gptq 050) onto v0.5.13 as the second pass — they were live in /data/vG, not in the setup series.
