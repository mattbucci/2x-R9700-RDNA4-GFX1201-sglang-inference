diff --git a/python/sglang/srt/multimodal/processors/qwen_vl.py b/python/sglang/srt/multimodal/processors/qwen_vl.py
index 7dd36c2dd..c40eda359 100644
--- a/python/sglang/srt/multimodal/processors/qwen_vl.py
+++ b/python/sglang/srt/multimodal/processors/qwen_vl.py
@@ -254,6 +254,14 @@ class QwenVLImageProcessor(SGLangBaseProcessor):
         if hf_config.model_type == "qwen3_omni_moe":
             hf_config = hf_config.thinker_config
 
+        # llmcompressor-saved configs can ship vision_config as a plain dict;
+        # downstream code (line 574, get_rope_index call) expects attribute
+        # access.  Wrap once, idempotently, at the point of config receipt.
+        from types import SimpleNamespace
+        vc = getattr(hf_config, "vision_config", None)
+        if isinstance(vc, dict):
+            hf_config.vision_config = SimpleNamespace(**vc)
+
         super().__init__(hf_config, server_args, _processor, *args, **kwargs)
 
         self.IM_START_TOKEN_ID = hf_config.vision_start_token_id
