# Experiments — 2x R9700 RDNA4 (r9700)

Vetted execution queue from the 2026-07-18 fleet audit. Each doc is a self-contained spec: a fresh agent
starts at Method step 1. The table preserves the audit-time ordering but reports current status; the
post-094 decision below is the current execution sequence.

> **Post-campaign assessment:** Each experiment now carries a dated current-state assessment immediately
> after its metadata table. Those comments supersede dependency and ordering text wherever they differ;
> each method and background remains intact as the audit receipt.

**Audit-time lane rationale:** C first: GPU-free (dataset dry-runs need HF network, not GPU; safe when no calibration is live) — fixes the silent audio/thinking calibration loss AND emits the drift manifest that documents the run_reap.py (→R97-F) and validate_capabilities.py (→R97-E) port sources. E: tool-call boot gate (3-5h) — unblocks post-quant gating that every future ship, including R97-F's AWQ, is validated against. D: tooluse probe + the two cross-team receipts 3090 owes an issue on (5-7h; freezes the 083-089 tree during A/B). A: EAGLE3 #52 depth curves (6-10h, downloads drafts) — feeds 3090 the RDNA4 curve + retrain gate. F: the REAP relay — gated on 3090-G's handoff, needs a DEDICATED flagship-offline window with the FP8 campaign paused, and consumes C's drift doc + E's gate. G: canonical cells — Phase A (aggregate rollup) is GPU-free and front-loads alongside C, Phase B/C are big and follow F. B and H last: both are needs-user-decision (decode-topk promotion; bench-dir hygiene) and only execute on Matt's call. All GPU-serving items serialize on the single TP2 pair around the in-flight 083-089 FP8 campaign.

## Post-094 execution decision

- **Completed locally:** R97-E's default-on tool-call gate and 17-preset sweep, R97-G Phase A's seven-cell
  rollup, and R97-C's calibration-source repair plus fingerprinted drift gate. R97-E qualified 16/17
  presets for structured calls and explicitly receipts GLM-4.5 Air as not agentic-qualified.
- **Completed North repair/gates:** R97-D's probe has 36 focused tests (42 with chart contracts), separate
  repeated-stress and heterogeneous agentic profiles, effective-seed receipts, and bounded raw failure
  diagnostics. Patches 090–094 repair North normalization, parser/finish semantics, SWA convention, and
  gfx1201 deterministic MM/BMM startup.
- **Active TP2 experiment:** R97-D (03): Laguna remains 7/7. North's pre-090 1/7 curve is superseded. The
  exact post-fix control scores heterogeneous code/log context 3/3 at both 64,801 and 115,806 tokens; the
  repository-native multi-turn focused gate passes 2/3 at both depths with 4/4 valid follow-ups using the
  tool result. The seven-rung, three-seed post-fix ladder is now the active GPU task.
- **Next existing performance decision:** R97-B (07) Option B is recommended, but no regeneration begins
  until the user records that choice. It remains full-attention-only and does not alter Laguna's default.
- **Deferred:** R97-A (04) for 16–64K AWQ speculative decode; R97-F (05) until the 3090-G handoff lands;
  R97-G Phase B/C until the short critical path is clear; R97-H (08) until its destructive scope is approved.

The repository-level rationale, completion criteria, and the missing direct native-FP8 follow-on are
documented in [`README.md`](../README.md#what-we-are-working-on-next).

| # | ID | Title | Type | Current status | Host | Wall clock |
|---|---|---|---|---|---|---|
| 01 | R97-C | [Propagate 3090 calibration-source fixes (8076b75) + add fleet drift-check for the five forked quant/eval scripts](01-calib-source-fixes-and-drift-check.md) | task | complete; redirects, live receipts, and fingerprinted drift gate pass | r9700-box | 3-6h (dry-runs are minutes each; drift script + receipts + docs the rest) |
| 02 | R97-E | [Port 3090's check_tool_call into the R9700 capability validator and gate all presets](02-toolcall-gate-validate-capabilities.md) | task | complete: 16/17 tool PASS; GLM exception receipted | r9700-box | completed in this campaign (fleet sweep 29:57 plus controls/triage) |
| 03 | R97-D | [Port and harden the 256K multi-turn tool-use probe; run the North/Laguna and cross-team controls](03-port-tooluse-probe-crossteam-abs.md) | experiment | **IN PROGRESS**: post-094 North focused gate passed; full three-seed ladder running | r9700-box | ~1-1.5 days (CPU port/tests complete; probe runs are prefill-dominated) |
| 04 | R97-A | [Wire delivered EAGLE3 drafts (Devstral-2, Qwen3-VL-32B) into the --spec lane + measure the true-depth curve (#52)](04-eagle3-dense-vl-spec-lane-depth-curve.md) | experiment | prep-ready; GPU deferred | r9700-box | 2-3 days (wiring+extraction day 1; depth curves day 2; receipts/decision day 3) |
| 05 | R97-F | [Qwen3.6-35B-A3B REAP 256->192e: prune on R9700, AWQ recal on 3090, serve+bench both rigs](05-qwen36-35b-reap192-two-rig-relay.md) | task | **BLOCKED: 3090-G handoff** | multi-rig | ~3-5 days end-to-end: ~0.5 day tooling port + gates (incl. Gate 2b slicer validation), ~0.5-1 day prune + verification (r9700-box), ~0.5 day HF transfer, ~1 day recal + convert (3090-calib-box), ~1-2 days serve/validate/bench + SWE-bench cell across both rigs. |
| 06 | R97-G | [Publish canonical-eval cells for North-Mini/Laguna + NGRAM-first specdecode.json rows](06-north-laguna-canonical-eval-ngram-rows.md) | task | Phase A complete (7 cells); B/C deferred | r9700-box | ~1-2 weeks elapsed: Phase A rollup complete; Phase B NGRAM ~1 day re-measurement on the current post-094 tree, then Phase C eval cells for the two opencode cells at SHARDS=1. |
| 07 | R97-B | [Decision brief: promote decode-topk (069) from CANDIDATE — promotion now requires a v0.5.15 regeneration, not a rename](07-decode-topk-promotion-brief.md) | decision-brief | NEEDS-USER-DECISION | r9700-box | Brief: done (this doc). If Option B approved: 0.5-1.5 days (regen + smoke + needle A/B + deep throughput-AND-needle re-gate; agentic re-gate adds ~4h). Estimate widened from 0.5-1 day per vetter: memory notes ~3 merge-remnants per rebase plus two full 256K boots. |
| 08 | R97-H | [Decision brief + execution: disposition 13 flagged legacy bench_serving dirs, de-twin stale April dirs](08-bench-dir-hygiene-purge-or-remeasure.md) | decision-brief | NEEDS-USER-DECISION | r9700-box | Option A (purge-only): ~1h. Option B (purge 11 + re-measure 2): ~1h cleanup + ~2-4h benching. |
| 09 | R97-I | [Per-model rung sets for the agentic tool-use ladder, then sweep the remaining qualified presets](09-fleet-ladder-per-model-rungs.md) | experiment | queued (spec only) | r9700-box | ~1 day: ~0.5 day CPU for the rung-set change and tests, then an overnight 14-preset sweep (~6-10h GPU) |
| 10 | R97-J | [Split KV in extend attention — cut the agentic tool-result turn tax at depth](10-extend-attention-kv-split-agentic-turn.md) | optimization | ready (cause measured, no fix) | r9700-box | ~1-2 days: kernel work + numerical gate, then A/B and a ladder re-run (~4-6h GPU) |
| 11 | R97-K | [Attribute the decode rocBLAS residue — which dense GEMMs never enter the FP8 path](11-decode-rocblas-residue-attribution.md) | investigation | ready (hypothesis from checkpoint config; unconfirmed) | r9700-box | ~0.5 day, mostly checkpoint/trace analysis; ~0-1h GPU only if the traces lack launch dims |
| 12 | R97-L | [Correctness-gated gfx1201 tuner for the normal block-FP8 GEMM at Laguna's shared-expert shapes](12-fp8-exact-shape-gemm-tuner.md) | optimization | ready (spec authored; FP8-native-follow-on steps 1-2 complete, exact-shape tuner not started) | r9700-box | ~1-2 days (kernel enum + gated sweep; ~4-8h GPU for sweep + deep A/B + re-eval) |
| 13 | R97-M | [Instrument GPU thermals/power/clock and re-measure deep decode at thermal steady state](13-gpu-thermal-power-clock-steady-state-soak.md) | investigation | ready (spec only; sampler unbuilt, passive — piggybacks any deep-decode window) | r9700-box | ~0.5-1 day (~1h sampler, then a >=10-min soak + shallow control; ~1-2h GPU) |
| 14 | R97-N | [Full-output-hash + determinism A/B gate and a depth-verified regression tripwire (baselines schema v2)](14-decode-ab-identity-and-depth-tripwire.md) | task | ready (spec only; mostly CPU + a short laguna window to arm baselines) | r9700-box | ~1-1.5 days (hashing + schema-v2 + tests on CPU; ~2-4h GPU to arm + backfill) |
| 15 | R97-O | [Re-measure concurrency and quantify head-of-line blocking on the post-095 native-FP8 tree](15-concurrency-hol-blocking-postfp8-remeasure.md) | experiment | queued (spec only; no execution started) | r9700-box | ~1-1.5 days (~4-6h GPU: concurrency curve + HoL probe + Overlap A/B) |

## Cross-rig edges touching this lane

| From | To | What moves |
|---|---|---|
| 3090-G | R97-F | REAP tooling handoff: staged byte-identical files (ream-patches/qwen3_5moe_unfused_experts.py, unified run_reap.py with pick_model_class/read_expert_counts composite-config fixes, test, Gate B/C/D scripts, doc edits) + Gate A transcript, copied from the 3090 checkout into the R9700 checkout. RESOLVED interface: R97-F consumes verbatim (Gate D diff→empty) and must NOT re-fork _PATCH_DIR (dual-dir resolution keys on FILE presence); R97-F's Gate 2 VERIFIES 3090-G's composite fixes on the real 67GB config instead of reimplementing them. |
| R97-F | 3090-calib-box | Pruned Qwen3.6-35B-A3B-REAP192-BF16 (~53GB) uploaded to HF (mattbucci/...REAP192-BF16, resumable per-file) + cross-team banner commit in the 3090 README → 3090-calib pulls for thinking_vision_video AWQ recal (RECIPE explicit — script default drops video; MoE-expert no-op grep; check_awq_scales.py --base against the pruned BF16). |
| R97-F | 3090 | Closes 3090 MoE-coverage backlog item 1: after recal, a qwen36-reap launch preset + official-Docker SWE-bench cell + depth-verified decode receipt land in the 3090 repo; the AWQ ship (mattbucci/Qwen3.6-35B-A3B-REAP-AWQ) is served+benched on both rigs. |
| 3090-F | R97-B | v0.5.15 Annotated-style 069 server_args rework + RDNA4/Gemma portability verdict pushed to the R9700 README. RESOLVED overlap: 3090-F (not user-gated) ships this reusable starting point first; R97-B (user-gated) adapts it for its 095-098 regen and re-anchors it against live patch 086 (both edit server_args.py) — the two files are NOT byte-identical (different patch stacks). |
| R97-A | 3090 | RDNA4 dense/VL EAGLE3 depth-curve receipt + the 16K VL-retrain gate decision via 3090 README cross-team note. CONDITIONAL (WIN + gate triggers): retrain ask to export the uncommitted chunked-vocab (zero-copy reduction-shift) SpecForge refactor from the 3090 training box's /data/specforge/SpecForge working tree — it exists nowhere else, so file the ask EARLY (reimage risk). |
| R97-D | 3090 | The two receipts 3090 explicitly requested: KV_DTYPE=auto-vs-fp8_e4m3 needle A/B (for 3090's prepared upstream issue — carry the patch-011 upcast-k caveat that voids their stale 'you are exposed' framing) and the nemotron ~113K FP8 spiral spot-check, plus a North/Laguna tool-use finding-share, via 3090 README reply. Donor: 3090's probe_256k_tooluse.py commit 5d32e1e read into R9700 (read-only). |
| R97-C | 3090 | copyheavy_decode_bench.py hardening (env-var SGLANG_DIR SRC_GLOB + 3600s cold-prefill D-state timeout) offered to 3090 whose copy hardcodes the stale /data/sglang-rebase-v0512 tree. Donor direction: 3090 commit 8076b75 (calibration-source redirects) read into R9700 for the targeted 3-entry port. |
| R97-E | 3090 | Parser-choice findings (qwen3vl-32b 'qwen' on R9700 vs 3090's 'qwen25' for the same family) + R9700-only template quirks (cohere_command4/poolside_v1 enable_thinking kwarg) via 3090 README cross-team note. Donor: 3090's check_tool_call read into R9700. |
| R97-G | 3090 | North-Mini/Laguna canonical SWE-bench cells scored with the official Docker harness (comparable instrument class) + the first NGRAM spec-decode datapoint on an SWA-MoE architecture (either confirms or breaks the RDNA4 deep-KV tree-verify collapse on windowed attention), via 3090 README. |

## Fleet sync points

- **reap-tooling-handoff** — participants: 3090-G, R97-F; trigger: 3090-G commits Gate-A-green (env transformers version recorded) + the staged byte-identical files on the 3090-calib-box; action: R97-F step-0: copy files into the R9700 checkout, run Gates B/C/D with empty HIP_VISIBLE_DEVICES/ROCR_VISIBLE_DEVICES (calibration-safe), commit those transcripts; THEN resolve the stage-2 full-CPU-reload OOM blocker (67GB BF16 vs 61GB RAM — shard-stream survivors or GPU+CPU max_memory) BEFORE spending the slow DeltaNet-torch-fallback observation pass; only then execute the 256→192 prune with --max-memory-per-gpu 28GiB (unified default is 20GiB).
- **pruned-bf16-to-recal** — participants: R97-F, 3090-calib-box; trigger: REAP192-BF16 HF upload complete + cross-team banner commit in the 3090 README; action: 3090-calib pulls the pruned BF16 and runs thinking_vision_video AWQ recal (RECIPE set explicitly, NUM_SAMPLES 256 escalating to 1024 only after a verified-free-RAM check, MoE-expert no-op grep, check_awq_scales --base clean); then both teams serve+bench the AWQ ship and publish the SWE-bench cell (promotion bar ≥162/300, kill <147).
- **docker-scoring-handoff** — participants: M4-D, 3090-B; trigger: M4-D callout committed with the resolved drop-dir qwen36-m4 (or 3090-B uses its in-spec corrected command); action: 3090-B confirms no live rollout (Rule 2), runs score_docker.py detached under the bake-off score lock, reads scores-docker-summary.json, commits it back to the M4 repo and ticks both READMEs' fleet-audit items.
- **depth-curve-to-retrain-decision** — participants: R97-A, 3090, Matt; trigger: R97-A's North/Laguna + Devstral/VL depth curves complete and the VL 16K-retrain gate is evaluated (finish_reason=length rungs excluded); action: if the gate triggers, file the SpecForge chunked-vocab export ask to 3090 EARLY (working tree only, reimage risk); retrain-host choice (their 24GB + refactor vs unproven SpecForge-on-ROCm) is a joint 3090+Matt call, execution out of scope.
- **069-v0515-rework-to-topk-decision** — participants: 3090-F, R97-B, Matt; trigger: 3090-F publishes the v0.5.15 server_args rework + portability verdict AND Matt answers R97-B's A/B/C; action: if Option B, R97-B regenerates 095-098 reusing 3090-F's Annotated-style rework, re-anchored against live 086, then runs the throughput-AND-needle re-gate at true 245K depth before wiring the opt-in knob.
- **baseline-schema-v2-to-sister-rearm** — participants: 3090-D, R9700, M4; trigger: 3090-D commits schema v2 and arms benchmarks/baselines.json (7 presets × 3 depths, tripwire fires both ways); action: R9700 and M4 re-arm their 2026-04-12 relics on their own depth-verified instruments (R9700 at short/medium/deep, M4 at ~32K with oom_guard). M4 leg complete (their baselines.json is `_meta.schema: 2` at genuine depth, saved 2026-07-19); R9700 leg = R97-N (doc 14).
- **calib-guard-defuse-gates-gemma-recal** — participants: 3090-H, R9700, Matt; trigger: 3090-H lands the manifest + calibration guards + BF16→AWQ symlink retarget on the 3090 hosts (Matt sign-off required ONLY for deletions); action: R9700 team defuses its host trap symlink + ports the guards; this must complete before ANY next gemma4-26b recalibration on either rig so the recal reads a real BF16 base, not double-quantized AWQ.
- **cross-team-tooluse-receipts** — participants: R97-D, 3090; trigger: R97-D's KV_DTYPE A/B + nemotron FP8 receipts committed with the patch-011 caveat; action: 3090 folds them into its prepared upstream issue / the 3090-I decision surface (the KV finding validates upcast-k as the ~free gfx1201 fix rather than an exposure).
- **canonical-cells-published** — participants: R97-G, 3090; trigger: R97-G Phase A rolls up the 7 existing Docker-scored cells and the North/Laguna opencode cells finish scoring; action: cross-team note to 3090 with the cells (labeled with the rollout-environment caveat: comparable scoring instrument, different rollout condition) + the NGRAM SWA-MoE datapoint.

## Audit-time starting state

- **3090-box**: 3090-C rewrite rules-for-agents.md + fix CLAUDE.md dead R9700 path (GPU-free docs, no deps), 3090-I author the upstream-PR + kernel-BUG decision brief (GPU-free; re-verify package staleness vs upstream main), 3090-A steps 1-8: author patch 058, write the committed unit test, run the 3-gate (GPU-free; bake-off cell queues behind the priority bake-off), 3090-H manifest generation + guard edits + symlink defuse on the 3090 eval box (GPU-free filesystem work)
- **3090-calib-box**: 3090-G author+refactor the unified Qwen3_5Moe REAP tooling (pick_model_class/read_expert_counts + byte-identical dual-dir resolution), run Gate A (CPU miniature + synthetic-composite test), stage byte-identical files for R97-F — highest cross-repo unblock; confirm no calibration in flight first
- **r9700-box**: R97-C calibration-source fixes (3-entry 8076b75 port) + fleet_drift_check.sh + KNOWN_DRIFT.tsv (GPU-free dataset dry-runs; only if no calibration/pruning is active, HF_HUB_OFFLINE unset), R97-G Phase A: extend aggregate_bakeoff.py and roll up the 7 existing Docker-scored cells into benchmarks/quality/ (GPU-free/CPU+docker, safe during any calibration) — all GPU-serving R9700 items queue around the in-flight 083-089 FP8 campaign on the single TP2 pair
- **m4-box**: DORMANT — no on-box work until Matt wakes the rig; the M4 lane (M4-F, M4-A, M4-B, M4-G, M4-E and M4-C steps 8-9) activates on wake
- **any-checkout (M4 repo-only prep)**: M4-C steps 1-7: pin --random-range-ratio 1 at all 5 bench_serving sites, port the actual-depth guard, flag the 10 instrument-A depth-suspect results.json + LEGACY-DEPTH-SUSPECT.md (leaving the /v1/completions instrument-B files untouched), M4-D: re-send the Docker-scoring ask as a callout in the 3090 README (corrected command, resolved qwen36-m4 drop-dir) + codify the receiving-repo routing rule in M4 CLAUDE.md

## Coordinator notes

INTERFACE DISAGREEMENTS RESOLVED: (1) 3090-G↔R97-F run_reap.py: R97-F's spec says 'change _PATCH_DIR to ream-patches', but 3090-G delivers byte-identical files with file-presence dual-dir resolution — 3090-G's design wins; R97-F copies verbatim (Gate D diff→empty), does NOT re-fork, and its Gate 2 VERIFIES rather than reimplements the composite-config fixes (pick_model_class, num_experts written at BOTH config levels, vision-tower preserved). 3090-G must land before R97-F's prune. (2) 3090-B↔M4-D drop-dir: 3090-B uses evals/swebench/m4-imports/qwen36-m4/, M4-D uses qwen36-m4-opencode/ — score_docker.py derives run_id from the parent dir so they must match; canonical = qwen36-m4 (3090-B is the executor), M4-D aligns and drops its variant. (3) 3090-F↔R97-B both regen 069 to v0.5.15: 3090-F (not user-gated) ships the reusable Annotated-style server_args rework first; R97-B (user-gated) adapts + re-anchors it for live 086 — files are NOT byte-identical (different patch stacks). (4) R97-C refreshed KNOWN_DRIFT.tsv after the local R97-E port: the validator now differs intentionally in both directions, and exact hashes/counts fail closed until a later full convergence is reviewed. run_reap.py convergence remains owned by R97-F/3090-G. (5) M4-F↔M4-B both edit setup.sh's patch loop: sequence M4-F (fail-hard loop + gates + version-header fix, glob stays 0[01][0-9]) THEN M4-B (adds patch 021, widens glob to [0-9][0-9][0-9], re-runs gates) — without the widen, 021 is silently skipped (the exact silent-failed-patch class the fleet was burned by).

CURRENT SCHEDULING CONSTRAINTS: (6) 3090-box GPU: the resuming SWE-bench bake-off (Active work #1) holds queue priority; 3090-A's cell, 3090-B (Rule 2 docker), and 3090-D/E/F slot into bake-off gaps, all detached via setsid and resumable across the ~9-17h kernel-BUG reboots. Calibration lives on the separate 3090-calib-box, so 3090-G and R97-F's recal run parallel to eval-box serving without tripping Rule 1. (7) r9700-box single TP2 pair: R97-E/D/A, R97-G Phase B/C, R97-F, and R97-B-exec all serialize; R97-F's prune takes North-Mini/Laguna offline ~1 day and requires a DEDICATED window after the FP8 baseline is checkpointed, distinct from R97-A/D/G (which serve those flagships), and must never interleave with serving; R97-D and R97-G freeze the post-089 working tree across their A/B pairs. (8) 3090-H: the BF16→AWQ symlink defuse + calibration guards must land before ANY next gemma4-26b recalibration on either rig (16h-loss double-quant class); manifest/defuse/guards proceed without Matt, only deletions wait.

MATT-GATED (nothing downstream moves without him): R97-B (decode-topk promotion A/B/C), R97-H (bench-hygiene Option A/B/keep + the two riders), R97-F GPU window + HF_TOKEN write access, 3090-I downstream actions (opening public PRs, sudo kernel/storage-driver work — one mechanism at a time, in a bake-off gap), 3090-H purge deletions, and R97-A retrain execution + host choice. The ENTIRE M4 box is dormant until Matt wakes it: only M4-C steps 1-7 and M4-D (repo-only, any-checkout) proceed now; M4-A/B/E/F/G and M4-C steps 8-9 activate on wake. Within M4, the bisect (M4-A) must precede any rebase (rebasing buries the evidence); M4-F is safe first (it does not rebase and provides the per-arm patch-chain gate M4-A reuses); M4-C's re-measure version confound (v0.5.11→v0.5.12) overlaps M4-A's bisect — feed the historical-'128K' instrument classification to M4-A rather than editing its charter. Also flag: 3090-I and R97-B both carry hardcoded upstream/commit hashes (27ad9d1 upstream; 127b9e328 for M4-F's v0.5.12 pin) that are unverifiable from the read-only checkouts — the executing teams must rev-parse on-box before writing them into any doc, and if they mismatch, the dependent bisect/PR items inherit the correction."
