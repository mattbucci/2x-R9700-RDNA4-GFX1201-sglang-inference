# R97-F: Qwen3.6-35B-A3B REAP 256->192e: prune on R9700, AWQ recal on 3090, serve+bench both rigs

| | |
|---|---|
| **Type** | task |
| **Status** | blocked — incomplete 3090-G handoff |
| **Execution host** | multi-rig |
| **Wall clock** | ~3-5 days end-to-end: ~0.5 day tooling port + gates (incl. Gate 2b slicer validation), ~0.5-1 day prune + verification (r9700-box), ~0.5 day HF transfer, ~1 day recal + convert (3090-calib-box), ~1-2 days serve/validate/bench + SWE-bench cell across both rigs. |
| **GPU time** | r9700-box: ~6-14h exclusive on both GPUs (saliency pass 1024 samples incl. video-routing rows on a 28GiB+28GiB+CPU-offload split, plus the coherence gate); 3090-calib-box: GPTQ recal is CPU-only per the qwen36/REAP precedent (CUDA_VISIBLE_DEVICES=\"\"), ~day-scale wall but ~0 GPU; serve+bench: ~2-4h GPU on each of 3090-box and r9700-box. |
| **Depends on** | 3090-G tooling handoff — NOT YET SATISFIED: the live donor still lacks the unified model/config helpers and dual-directory resolution required by the current assessment below.; R9700 GPU window: user/team scheduling call for a dedicated ~1-day prune occupancy after the FP8 baseline is checkpointed.; 3090-calib-box availability + Rule 1 window for the ~day-scale GPTQ recal (3090 team executes steps 9-13).; HF_TOKEN with write access to mattbucci/* for the two uploads. |
| **Provides to** | 3090 repo: closes MoE-coverage-backlog item 1 (Qwen3.6-35B-A3B-REAP-AWQ) — new qwen36-reap preset, bake-off cell, decode receipt.; R9700 repo: a lighter 192-expert AWQ serving option under the qwen36-moe preset (KV/load headroom on 64 GB) + committed, re-validated Qwen3_5Moe REAP tooling reusable for future fused-MoE prunes.; Fleet: mattbucci/Qwen3.6-35B-A3B-REAP-AWQ + REAP192-BF16 on HF as shared artifacts. |

## Current assessment — 2026-07-18 post-089

- **Disposition:** **Blocked.** The earlier satisfied-handoff claim was stale. At donor HEAD `5d46fb3`,
  `run_reap.py` still lacks `pick_model_class`, `read_expert_counts`, file-presence dual-directory
  resolution, nested-config writes, and crash-safe pre-save reporting. The matching R9700 helper and
  acceptance test are also absent.
- **Goal fit:** Low priority for the current FP8/256K speed objective. This lane creates an AWQ
  footprint/KV-headroom option and explicitly expects M=1 decode parity, not higher throughput.
- **Blocking gates:** 3090-G must finish and pass its tooling Gate A; the shared files must then be staged
  byte-identically for R9700 Gates B/C/D and Gate 2b. An HF write credential and an explicitly approved,
  exclusive TP2 prune window remain additional prerequisites.
- **Next action:** Do not copy the current donor. Keep R97-F blocked until the donor defines both
  `pick_model_class` and `read_expert_counts`, implements the agreed dual-directory/nested-config behavior,
  and passes its synthetic-composite test. Only then perform the R9700 CPU gates and request a GPU window.

## Objective

Complete the fleet's MoE coverage matrix item 1 (Qwen3.6-35B-A3B in native + REAP + REAM) by running the 256-to-192-expert REAP prune on the R9700 box — the named better prune host (64 GB VRAM vs the 3090's CPU-offload risk) — using the 3090's delivered Qwen3_5Moe unfuse tooling, then relaying the pruned BF16 to the 3090 calibration owner for a thinking+vision+video AWQ recal and serving/benching the ship on both rigs. The base is the bake-off top scorer (177/300), so the REAP variant extends the fleet's best agentic model with a smaller-footprint 256K option.


## Hypothesis

n/a (build/relay task; the embedded quality expectation — REAP-192 retains >=native-minus-5-pts SWE-bench and twin-band 255K decode with thinking+vision+video preserved — is expressed as measurable success/kill gates)


## Background & receipts

- 3090-G handoff is DELIVERED in the 3090 checkout: patches/qwen3_5moe_unfused_experts.py (fused-3D load-split + save-fuse hooks), run_reap.py Qwen3_5Moe support (_gate_num_experts, tuple-router saliency branch, custom-router in-place prune), scripts/quantize/test_qwen3_5moe_unfuse.py — commits 708a694, 9030c8e, d554fdb; 3090 README backlog item 1 (line 64) confirms 'miniature-validated 7/7; on-box run pending' and explicitly prescribes the AWQ recal use RECIPE=thinking_vision_video + check_awq_scales.py --base.
- R9700's local copies are STALE: scripts/quantize/run_reap.py (383 lines vs 3090's 454) lacks all Qwen3_5Moe handling and loads helpers from ream-patches/; ream-patches/ currently holds qwen3moe_unfused_experts.py AND glm4moe_unfused_experts.py (plus two .patch files + README) but NOT the required qwen3_5moe_unfused_experts.py, which must be ported in. run_reap.sh defaults REAP_ENV=vllm, an env that no longer exists on this box.
- Env probe (this box, /home/letsrtfm/miniforge3/envs verified): the seven envs are fp8-quant, nemo-quant, sglang-0511-bisect, sglang-triton36, sglang-triton36-v0513, sglang-triton36-v0514, sglang-triton36-v0515 — there is NO vllm env (the load-bearing fact). nemo-quant = transformers 5.5.4 + torch 2.11.0+rocm7.2 + accelerate 1.12.0 + datasets 4.6.0 and HAS Qwen3_5MoeExperts + Qwen3_5MoeTopKRouter; fp8-quant (tx 4.57.6) has NO transformers.models.qwen3_5_moe module and cannot load Qwen3.6 — nemo-quant is the only viable prune env.
- Base model is local: /home/letsrtfm/AI/models/Qwen3.6-35B-A3B-BF16 = 66.97 GiB safetensors, 26 shards, arch Qwen3_5MoeForConditionalGeneration, text_config.num_experts=256, num_experts_per_tok=8, 40 layers, plus preprocessor_config.json + video_preprocessor_config.json + chat_template.jinja — a vision+VIDEO model, so every downstream calibration/coherence stage must exercise the video path, not just image.
- Host limits (verified free/df): 61 GB RAM, 0 swap; root fs 99% full (22G free) but /data has 895G free — the delivered run_reap.py stage-2 save (lines 414-420) reloads the FULL BF16 to CPU (device_map=None, low_cpu_mem_usage=False) which cannot fit 67 GiB in 61 GB RAM with 0 swap (GLM device_map=cpu thrash precedent, project-glm-ownbuild-inflight.md); save stage must be adapted before launch.
- Nested-config gap in the delivered script (verified): run_reap.py line 348 reads getattr(model.config,'num_experts',None) or num_local_experts — this base stores num_experts=256 only under text_config, so if the composite config surfaces, num_experts resolves None and the 'keep_experts >= num_experts' check (line 350) raises TypeError. Additionally the save stage (line ~427) writes model.config.num_experts=keep only at TOP level (and num_local_experts if present) — never under text_config — so the pruned checkpoint would carry a stale text_config.num_experts=256. Both must be preflighted/patched: read via config.get_text_config() and WRITE 192 at BOTH config levels on save.
- MoE-expert GPTQ no-op trap (project-glm-ownbuild-inflight.md, 3090 commit f10074a): GPTQModifier(targets='Linear') silently skips fused 3-D expert Parameters (North-Mini v3: 199/12491 Linears, 29.5h wasted); Qwen3.5/3.6 ship FUSED experts on disk, so the recal output must be checked for quantized expert weights, not just exit status.
- AWQ recal interface (3090 side, verified): scripts/quantize/quantize_qwen36_thinking_vision.py (BASE_MODEL/OUTPUT_DIR/NUM_SAMPLES/RECIPE env overrides; DEFAULT recipe is thinking_vision which has NO video data — must be overridden to thinking_vision_video, present in calibration_datasets.py line 479 = adds llava_video_178k; header notes 256-sample runs under-calibrated rare experts — 144 flagged scales on the shipped native AWQ — recommends NUM_SAMPLES=1024 only 'if RAM budget allows', warns <70 GB tight); convert_moe_ct_to_awq.py <src> <dst>; scripts/eval/check_awq_scales.py (491-line file; <awq-dir> --base <bf16-base-dir> usage documented in header ~line 32; dead-channel comparator); REAP-recal precedent quantize_qwen35_28b_moe_reap_thinking.py (54 GB REAP base, CPU-only CUDA_VISIBLE_DEVICES="", ~3-6h).
- Serving/bench interfaces verified: R9700 launch.sh qwen36-moe preset (moe_wna16 auto-detect, CTX=262144, DECODE_STEPS=8, cuda-graph ON, comment states REAM-A3B is served via this preset with a MODEL= override — same path works for REAP; confirm launch default PORT=23334 or pass --port to match validate_capabilities canonical 23334); 3090 launch.sh qwen36-ream preset (awq_marlin, 192-expert twin, 144 t/s @255K); R9700 scripts/eval/validate_capabilities.py + scripts/bench/bench_256k_sweep.sh; 3090 scripts/eval/validate_capabilities.py (thinking/tool_call/vision/video gates) + evals/swebench/run_all_cycles.sh; 3090 qwen36 native cell = 177/300 (59.0%) SWE-bench, 121 t/s @255K.
- Not redundant: mattbucci Qwen3.6-REAM-A3B (192e, Samsung SAIL merge) already ships, but the coverage matrix requires native + REAP + REAM per MoE base — Qwen3.6-35B-A3B-REAP-AWQ is 3090 backlog item 1 and both rigs' READMEs name R9700 the better prune host (64 GB VRAM vs 3090 CPU-offload risk).
- Cross-team transfer channel (reference-sister-teams.md): HF ships at huggingface.co/mattbucci + '> Cross-team request from R9700 (date)' README banner; scripts/quantize/upload_repo_per_file.py exists in both repos for resumable per-file upload.
- Scheduling: R9700 working tree carries the completed-but-uncommitted FP8 baseline (patches 083-089, new benchmarks/fp8-256k-options-r9700-2026-07-18.json); CLAUDE.md line 50: no serving or GPU benchmarks while calibration or pruning is active; line 60: detach prune jobs in their own session (setsid). The step-1 tooling-port commit MUST stage only ream-patches/ + scripts/quantize/ + run_reap.sh and MUST NOT stage the FP8 baseline files.


## Method

1. Port tooling (r9700-box, no GPU, git-tracked): copy 3090 patches/qwen3_5moe_unfused_experts.py into R9700 ream-patches/; adopt the 3090 scripts/quantize/run_reap.py wholesale (it is a superset of R9700's 383-line copy: _gate_num_experts, Qwen3_5MoeTopKRouter tuple-output saliency branch, custom-router in-place prune, --max-memory-cpu auto) changing _PATCH_DIR from 'patches' to 'ream-patches' and importing the unfuse modules; copy test_qwen3_5moe_unfuse.py with the same path fix; change run_reap.sh REAP_ENV default from the nonexistent 'vllm' env to 'nemo-quant'. Commit boundary: `git add` ONLY ream-patches/ scripts/quantize/ run_reap.sh — never stage the uncommitted FP8 campaign files (patches 083-089, benchmarks/fp8-256k-options-r9700-2026-07-18.json).
2. Gate 1 — miniature unfuse equivalence: /home/letsrtfm/miniforge3/envs/nemo-quant/bin/python scripts/quantize/test_qwen3_5moe_unfuse.py must exit 0 on THIS box (re-validates the 3090's 7/7 under tx 5.5.4/rocm torch — their env may differ; any assert failure is a tx-skew finding, fix before proceeding).
3. Gate 2 — wrapper-arch preflight (minutes, no weight load): with accelerate init_empty_weights build the model from /home/letsrtfm/AI/models/Qwen3.6-35B-A3B-BF16/config.json in nemo-quant; confirm which Auto class resolves Qwen3_5MoeForConditionalGeneration, that router paths end '.mlp.gate' and the '.layers.N.' index parse survives any 'language_model.' prefix, and FIX the verified num_experts gap: config stores num_experts=256 only under text_config, while run_reap.py line 348 reads model.config.num_experts (None -> TypeError at line 350) and line ~427 writes it only at TOP level; patch to READ via config.get_text_config() and to WRITE num_experts=192 at BOTH config levels on save.
4. Gate 2b — stream-prune slicer correctness (BEFORE the full prune, new code needs a tripwire): build a 2-layer toy Qwen3_5Moe checkpoint, run step-4a's safetensors index_select slice logic on it, reload, and compare logits against the Gate-1-validated delivered save_pretrained path on the SAME survivor lists (max abs logit delta ~0). If they diverge, either fix the slicer or fall back to making the swapfile + delivered save_pretrained the PRIMARY save and demote stream-prune to an optimization. This validates the actual save mechanism before the multi-hour run — the coherence gate alone (post-prune) is too late to catch a silent slice corruption cheaply.
5. Fix the save stage BEFORE launch (verified blocker: stage-2 reloads the full BF16 to CPU with low_cpu_mem_usage=False — 66.97 GiB safetensors vs 61 GB RAM, 0 swap): (a) HARD ORDERING GATE — dump survivors+saliency to reap_report.json IMMEDIATELY after the [4/4] survivor computation and BEFORE any slicing (crash-safe: a corrupt-save retry must never re-run the saliency pass); then stream-prune at safetensors level: for each of the 26 shards, index_select the fused experts.gate_up_proj [256,1024,2048] / experts.down_proj [256,2048,512] on dim 0 and the router gate.weight rows by each layer's survivor list, copy all non-expert tensors (incl. the vision tower) untouched, rewrite shards + model.safetensors.index.json + config (num_experts=192 at both levels). (b) fallback — temporary swapfile on /data (GLM precedent) + low_cpu_mem_usage=True reload via the delivered save_pretrained, accepting a slow save. Never use device_map='auto' for the save reload (documented meta-tensor save_pretrained crash). Gate 2b determines which of (a)/(b) is primary.
6. Run the prune (r9700-box, both GPUs, flagships offline per CLAUDE.md no-serving-during-pruning; coordinate the window against the uncommitted FP8 campaign): setsid env REAP_ENV=nemo-quant ./scripts/quantize/run_reap.sh --model ~/AI/models/Qwen3.6-35B-A3B-BF16 --save-path /data/models/Qwen3.6-35B-A3B-REAP192-BF16 --keep-experts 192 --max-memory-per-gpu 28GiB --num-samples 1024 > /data/tmp/reap-qwen36.log 2>&1. Saliency data: default theblackcat102/evol-codealpaca-v1 (shipped Coder-30B-REAP precedent) MUST be augmented with vision-describing + video-routing samples so the survivor set does not starve the multimodal paths (text-only saliency risks pruning experts the vision/video paths route to, surfacing only at the post-AWQ modality gate). Output goes to /data (895G free) — root fs holding ~/AI/models has 22G free and the pruned BF16 is ~53 GiB.
7. Verify + gate the pruned artifact: reap_report.json shows 192 survivors on every MoE layer; num_experts=192 at BOTH config levels (top + text_config); file-list diff vs the base carries chat_template.jinja, preprocessor_config.json, video_preprocessor_config.json, generation_config.json and all vision-tower tensors (run_reap saves model+tokenizer only — copy any missing aux files explicitly). Then a transformers-level coherence gate on the GPUs (GLM precedent): thinking + text QA rounds PLUS one image round AND one VIDEO round via the processor (video is a ship-gating modality — do not skip it); ~53 GiB BF16 fits the 2x28GiB budget. Record as a dated receipt.
8. Handoff to the calibration owner: python scripts/quantize/upload_repo_per_file.py --repo mattbucci/Qwen3.6-35B-A3B-REAP192-BF16 --src /data/models/Qwen3.6-35B-A3B-REAP192-BF16 (resumable per-file), then push a '> Cross-team request from R9700 (2026-07-XX)' banner + this spec's pointers to the 3090 README and commit (the receipted cross-team channel).
9. 3090-calib-box recal (3090 team, Rule 1 honored by running on the calib device): BASE_MODEL=<local pruned dir> OUTPUT_DIR=$MODELS_DIR/Qwen3.6-35B-A3B-REAP-AWQ-CT RECIPE=thinking_vision_video CUDA_VISIBLE_DEVICES="" python scripts/quantize/quantize_qwen36_thinking_vision.py — RECIPE=thinking_vision_video set EXPLICITLY (the script default is thinking_vision, which has NO video data; the video recipe is prescribed by 3090 README backlog item 1 and present at calibration_datasets.py line 479); confirm build_calibration_dataset accepts it and includes video rows before launch. NUM_SAMPLES: default to 256 (the shipped native used 256 and check_awq_scales --base cleared its 144 flags) and escalate to NUM_SAMPLES=1024 ONLY after `sync; echo 1 > /proc/sys/vm/drop_caches` + a free-RAM check confirms headroom on the ~62 GB box (the script warns <70 GB is tight; 1024 ~= 4x Hessian RAM 'if RAM budget allows'). CPU-only per the Qwen3.5-28B-REAP precedent (54 GB base ran ~3-6h; this base is ~53 GiB).
10. Gate 3 — MoE-expert GPTQ no-op guard + scales (3090): the pruned checkpoint is FUSED Qwen3_5Moe, the exact shape class of the North-Mini 199/12491-Linear silent no-op; after the run, grep the output safetensors index for quantized expert weights (experts.*weight_packed / qweight present in every MoE layer) and fail loudly if absent; then python scripts/eval/check_awq_scales.py <output> --base /path/to/Qwen3.6-35B-A3B-REAP192-BF16 (usage per header ~line 32 of the 491-line file) — dead-channel comparator must be clean and no worse than the native-AWQ 144-flagged-scales baseline.
11. Convert + ship (3090): python scripts/quantize/convert_moe_ct_to_awq.py <CT dir> $MODELS_DIR/Qwen3.6-35B-A3B-REAP-AWQ (consult convert_qwen35_moe_reap_ct_to_awq.py for REAP-specific handling); bundle chat_template.jinja + preprocessor/video_preprocessor configs; upload as mattbucci/Qwen3.6-35B-A3B-REAP-AWQ via upload_repo_per_file.py.
12. Serve+bench on 3090: add a qwen36-reap preset cloned from qwen36-ream (same 192-expert arch, awq_marlin, cuda-graph settings) pointing at the REAP-AWQ; python scripts/eval/validate_capabilities.py full gate (thinking/basic/tool_call/vision/VIDEO); depth-verified decode per their bench-depth-bug-2026-07-14.md method vs twins qwen36-ream (144 t/s @255K) and qwen36 native (121 t/s @255K); SWE-bench Lite cell via evals/swebench/run_all_cycles.sh vs native 177/300, honoring Rule 2.
13. Serve+bench on R9700 (after the FP8-campaign window reopens): MODEL=$MODELS_DIR/Qwen3.6-35B-A3B-REAP-AWQ ./scripts/launch.sh qwen36-moe (receipted REAM-A3B precedent: this preset serves 192-expert variants via MODEL= override; moe_wna16 auto-detected); confirm the server is on port 23334 (pass --port 23334 to launch.sh if its default differs from the validator's canonical 23334); python scripts/eval/validate_capabilities.py --port 23334 (thinking/basic/tool_call/vision/VIDEO); bash scripts/bench/bench_256k_sweep.sh vs the incumbent qwen36-moe receipts; expect M=1 decode parity (3090 finding: REAP does not speed M=1 decode — the win is KV/load headroom + coverage-matrix completeness), record receipts + README rows on both rigs.


## Baseline & instrument

Pre-registered comparison points, all existing receipts: 3090 qwen36 native = 177/300 SWE-bench Lite (Docker harness) and 121 tok/s @255K server-verified; 3090 qwen36-ream (192e twin) = 144 tok/s @255K (README performance table, bench-depth-bug-2026-07-14.md method); R9700 qwen36-moe = current scripts/bench/bench_256k_sweep.sh receipts in benchmarks/ (re-run one fresh sweep on the incumbent before the REAP A/B); scales baseline = 144 flagged scales on shipped native AWQ at NUM_SAMPLES=256 (quantize_qwen36_thinking_vision.py header).


## Success criteria

- Gate 1: test_qwen3_5moe_unfuse.py exits 0 in the R9700 nemo-quant env (receipt: command output in the commit/PR notes).
- Gate 2b: the stream-prune slicer's reloaded logits match the delivered save_pretrained path on a 2-layer toy checkpoint (receipt: max-abs-logit-delta ~0) — OR the swapfile save_pretrained path is adopted as primary instead.
- Pruned BF16 exists at /data/models/Qwen3.6-35B-A3B-REAP192-BF16 with num_experts=192 at BOTH config levels, per-layer 192-survivor reap_report.json (dumped before slicing), full aux-file parity vs the base, and passes the transformers-level coherence gate: thinking + text QA + one image round + one VIDEO round.
- Recal receipts on 3090: recal ran with RECIPE=thinking_vision_video; quantized expert weights present in every MoE layer of the output index (no-op-trap grep); check_awq_scales.py --base <pruned BF16> reports 0 dead-channel flags and no worse than the native-AWQ 144-flagged-scales baseline overall.
- validate_capabilities.py full pass on BOTH rigs at CTX=262144 — thinking, basic, tool_call, vision, VIDEO (R9700: --port 23334 run log; 3090: their gate log).
- Depth-verified decode within 10% of each rig's 192-expert twin at ~255K from server-log gen-throughput (3090: vs qwen36-ream 144 t/s; R9700: vs its own qwen36-moe bench_256k_sweep.sh receipt — parity expected, not speedup).
- SWE-bench Lite promotion bar: >=162/300 (native 177/300 minus 5 pts) on the 3090 Docker harness promotes the REAP-AWQ into the coverage matrix. A result of 147-161/300 (below the bar, above the kill line) is published in the bake-off table as a negative finding and NOT promoted; <147/300 is a kill (see kill criteria). The cell is published in the bake-off table in every case.


## Kill criteria

- Observation pass shows the GLM stall signature (no per-sample progress >1h, swap-thrash/RSS pinned at RAM ceiling): kill, lower --max-memory-cpu, retry once; second stall = record null with the log as receipt.
- Stream-prune save hits an expert/router tensor layout the shard-level slicer can't handle AND the swapfile fallback also fails: park the prune, record the layout finding (survivors JSON is already persisted from the pre-slice dump — the observation pass is not lost).
- Post-AWQ validate_capabilities fails vision OR video on either rig, and a single re-prune with mixed thinking+vision-describing+video-routing saliency text still fails: record null 'REAP-192 breaks Qwen3.6 multimodal routing' and stop.
- Prune occupancy exceeds 2 days on R9700 (blocking the FP8 campaign): checkpoint the saliency report and reschedule the window.
- SWE-bench Lite cell drops >10 pts vs native 59.0% (i.e. <147/300): record a null and do NOT promote; publish the cell as a negative finding with receipts. (The 147-161 band — below the 162 promotion bar but above the 147 kill line — is NOT a kill: publish-as-negative, do not promote to the coverage matrix.)


## Deliverables

- R9700 repo: ream-patches/qwen3_5moe_unfused_experts.py (ported), updated scripts/quantize/run_reap.py (Qwen3_5Moe support + nested-config read/write fix at BOTH config levels + crash-safe survivors dump + RAM-safe stream-prune save stage), scripts/quantize/test_qwen3_5moe_unfuse.py, run_reap.sh REAP_ENV default fix — committed in a single tooling-only commit that touches ream-patches/ + scripts/quantize/ + run_reap.sh and nothing else.
- /data/models/Qwen3.6-35B-A3B-REAP192-BF16 (~53 GiB, 192 experts/layer, text_config.num_experts=192, full aux-file set) + reap_report.json (per-layer survivors + saliency, dumped BEFORE any slicing) + /data/tmp/reap-qwen36.log.
- Pruned-BF16 coherence receipt (transformers-level: thinking + text QA + one image round + one VIDEO round) recorded in R9700 benchmarks/ as a dated .md/.json.
- HF: mattbucci/Qwen3.6-35B-A3B-REAP192-BF16 (handoff) and mattbucci/Qwen3.6-35B-A3B-REAP-AWQ (ship); cross-team request banner commit in the 3090 README.
- 3090: Qwen3.6-35B-A3B-REAP-AWQ-CT + converted AWQ dir, check_awq_scales --base receipt, quantized-expert-count receipt (safetensors index grep), new qwen36-reap launch.sh preset, validate_capabilities pass log (incl. video), depth-verified decode receipt, SWE-bench Lite cell in the bake-off table.
- R9700: validate_capabilities pass log (incl. video, --port 23334) + bench_256k_sweep.sh receipts for the REAP-AWQ under the qwen36-moe preset (MODEL= override), added to benchmarks/ + README table.


## Constraints

- R9700: no serving or GPU benchmarks while the prune is active (CLAUDE.md line 50) — flagships North-Mini/Laguna offline for the prune window; schedule only after the FP8 baseline is checkpointed and do NOT clobber the working tree (patches 083-089 are the current state). The step-1 port commit stages ONLY ream-patches/ + scripts/quantize/ + run_reap.sh — never patches 083-089 or benchmarks/fp8-256k-options-r9700-2026-07-18.json.
- Detach the prune via setsid with a /data/tmp log (CLAUDE.md line 60; feedback-detach-no-concurrent-calib).
- All prune outputs and swap/scratch go to /data (895G free) — root fs is 99% full (22G free); a save into ~/AI/models (on /) wedges the box.
- 3090 Rule 1: recal runs on the 3090-calib-box, never concurrent with serving/eval on the eval box; Rule 2: no concurrent rollout+score for the SWE-bench cell.
- Preserve thinking+vision+VIDEO through every artifact (feedback-preserve-modalities; 3090 CLAUDE.md forbids calibrating a video-capable model without video samples — temporal-attention drift): carry chat_template.jinja + preprocessor_config.json + video_preprocessor_config.json + vision tower weights in the pruned BF16; RECIPE=thinking_vision_video at recal (NOT the script default thinking_vision, which drops video); include video-routing samples in the saliency pass; exercise a video round in the coherence gate; full thinking+image+VIDEO modality gate before ship.
- Measurement: decode tok/s from server-log gen-throughput at true depth only (bench_256k_sweep.sh / 3090 depth-verified method); one mechanism at a time — the only variable vs the qwen36-ream/qwen36-moe baselines is the checkpoint.
- Prune-ourselves rule: base is the upstream BF16 already on disk; no third-party pruned/quantized intermediates.
- Repo hygiene: ported tooling lands as tracked files in scripts/quantize/ + ream-patches/ with the run_reap.sh env-default fix committed; negative results are findings with receipts.


## Risks

- transformers skew: the unfuse patch was validated in the 3090's quant env; R9700's nemo-quant is tx 5.5.4 — Qwen3_5MoeExperts internals could differ (Gate 1 miniature test catches this in minutes before any big run).
- Text-only saliency (evol-codealpaca) may prune experts the vision/video paths route to — mitigated by augmenting the saliency pass with vision-describing + video-routing samples (step 5) and bounded by the single re-prune kill criterion; residual risk surfaces only at the post-AWQ modality gate.
- Stream-prune save is new code over 26 shards — a wrong slice silently corrupts the checkpoint; Gate 2b (miniature slice-vs-save_pretrained logit equivalence) validates the actual save path BEFORE the multi-hour run, and the coherence gate + reap_report cross-check are the post-run tripwire before any handoff.
- Auto-class/nested-config behavior on the ConditionalGeneration wrapper is env-dependent; Gate 2 preflight resolves it cheaply, but an unexpected module layout could push real porting work.
- 3090-calib-box RAM is ~62 GB and quantize_qwen36_thinking_vision.py warns below 70 GB; the ~53 GiB pruned base plus Hessians is tight — default NUM_SAMPLES=256 (native precedent cleared its 144 flags at 256) and escalate to 1024 only after a drop_caches + verified-free-RAM check (step 9).
- ~53 GiB HF upload is hours-scale on home bandwidth; upload_repo_per_file.py resumes per-file but the relay wall-clock depends on it.
- R9700 GPU-window contention with the FP8 campaign — a mid-prune preemption wastes the saliency pass unless the crash-safe survivors dump (step 4a, fired before any slicing) has already persisted reap_report.json.


---
*Vetted 2026-07-18: drafted against live repo state, adversarially checked (feasibility + design), revised. Part of the fleet-audit experiment queue.*
